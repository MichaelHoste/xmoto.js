// Generated by CoffeeScript 2.7.0
(function() {
  var Assets, Blocks, Camera, Constants, Edges, EdgesWithAngle, Entities, Ghost, Ghosts, Infos, Input, Layers, Level, Limits, Listeners, Math2D, Moto, MotoFlipService, Particles, Physics, Replacements, Replay, ReplayConversionService, Rider, Script, Sky, Theme, b2AABB, b2Body, b2BodyDef, b2CircleShape, b2DebugDraw, b2EdgeChainDef, b2EdgeShape, b2Fixture, b2FixtureDef, b2MassData, b2MouseJointDef, b2PolygonShape, b2PrismaticJointDef, b2RevoluteJointDef, b2Settings, b2Vec2, b2World, bind_debug_button, bind_delete_params_buttons, create_form_with_url_params, display_constants, override_constants_by_url_params;

  Camera = class Camera {
    constructor(level) {
      this.level = level;
      this.options = level.options;
      // level unities * scale = pixels
      this.scale = {
        x: Constants.default_scale.x,
        y: Constants.default_scale.y
      };
      // x and y translate on the target view
      this.translate = {
        x: 0,
        y: 0
      };
      this.aabb = new Box2D.Collision.b2AABB();
    }

    init() {
      if (Constants.manual_scale) {
        this.init_scroll();
      }
      if (Constants.debug_culling) {
        this.culling = new PIXI.Graphics();
        this.culling.label = 'culling (window)';
        this.culling.alpha = 0.2;
        this.level.layers.translate_layer.addChild(this.culling);
      }
      return this.compute_aabb();
    }

    active_object() {
      if (this.level.options.playable) {
        return this.level.moto.body;
      } else {
        return this.level.ghosts.player.moto.body;
      }
    }

    move() {
      var speed, velocity;
      if (Constants.automatic_scale) {
        velocity = this.active_object().GetLinearVelocity();
        speed = Math2D.distance_between_points(new b2Vec2(0, 0), velocity);
        this.scale.x = this.scale.x * 0.995 + (Constants.default_scale.x / (1.0 + speed / 7.5)) * 0.005;
        this.scale.y = this.scale.y * 0.995 + (Constants.default_scale.y / (1.0 + speed / 7.5)) * 0.005;
        this.translate.x = this.translate.x * 0.97 + velocity.x / 3.0 * 0.03;
        this.translate.y = this.translate.y * 0.99 + velocity.y / 3.0 * 0.01;
      }
      return this.compute_aabb();
    }

    update() {
      var aabb_factor, ctx, cx, cy, delta_x, effective_scale_x, effective_scale_y, half_h, half_w, hh, hw, i, k, len, level_left, level_top, parallax, ref, scale_layer, size_x, size_y, t, translate_layer, z;
      if (Constants.debug_physics) {
        ctx = this.level.physics.debug_ctx;
        ctx.save();
        ctx.translate(this.options.width / 2, this.options.height / 2); // Center of canvas
        ctx.scale(this.scale.x, this.scale.y); // Scale (zoom)
        ctx.translate(-this.target().x, -this.target().y); // Camera on moto
        this.level.physics.world.DrawDebugData();
        return ctx.restore();
      } else {
        t = this.target();
        this.level.layers.scale_layer.x = this.options.width / 2;
        this.level.layers.scale_layer.y = this.options.height / 2;
        this.level.layers.scale_layer.scale.x = this.scale.x;
        this.level.layers.scale_layer.scale.y = -this.scale.y;
        this.level.layers.translate_layer.x = -t.x;
        this.level.layers.translate_layer.y = t.y;
        // Containers for parallax scale/translate transforms
        delta_x = 5; // Special delta so that x parallax is exactly the same as original XMOTO
        level_left = this.level.limits.player.left + delta_x;
        level_top = this.level.limits.player.top;
        aabb_factor = Constants.debug_culling ? 0.6 : 1.0;
        ref = this.level.layers.list;
        for (i = k = 0, len = ref.length; k < len; i = ++k) {
          parallax = ref[i];
          scale_layer = this.level.layers.parallax_scale_layers[i];
          translate_layer = this.level.layers.parallax_translate_layers[i];
          scale_layer.x = this.options.width / 2;
          scale_layer.y = this.options.height / 2;
          // Blend between the camera's current scale and the default scale based on
          // parallax depth (z). z=1 (parallax=1,1) follows the camera zoom like the
          // main level; z=0 (distant parallax) keeps a constant default scale.
          z = (parallax.x + parallax.y) / 2.0;
          effective_scale_x = z * this.scale.x + (1.0 - z) * Constants.default_scale.x;
          effective_scale_y = z * this.scale.y + (1.0 - z) * Constants.default_scale.y;
          scale_layer.scale.x = effective_scale_x;
          scale_layer.scale.y = -effective_scale_y;
          // Half-viewport in this parallax layer's world coords, derived from its
          // effective scale (NOT the default scale — each layer has its own zoom).
          half_w = this.options.width / (2.0 * effective_scale_x);
          half_h = this.options.height / (2.0 * Math.abs(effective_scale_y));
          // Anchored to the level's top-left corner so that parallax=1 matches the
          // main level (translate=(-t.x, t.y)) and parallax=0 stays frozen there.
          // The Y has an extra (1-parallax.y) * half_h correction because parallax
          // layers anchor at the viewport TOP edge, not its centre.
          translate_layer.x = -(level_left + parallax.x * (t.x - level_left));
          translate_layer.y = level_top + parallax.y * (t.y - level_top) - half_h * (1.0 - parallax.y);
          // Per-layer AABB in world coords, used by blocks/edges for visibility culling.
          // Centre matches the parallax viewport centre; size matches its effective scale.
          cx = -translate_layer.x;
          cy = translate_layer.y;
          hw = half_w * aabb_factor;
          hh = half_h * aabb_factor;
          if (parallax.camera_aabb == null) {
            parallax.camera_aabb = new Box2D.Collision.b2AABB();
          }
          parallax.camera_aabb.lowerBound.Set(cx - hw, cy - hh);
          parallax.camera_aabb.upperBound.Set(cx + hw, cy + hh);
        }
        // Opaque culling to see where sprites are "filtered out"
        if (Constants.debug_culling) {
          size_x = this.options.width / this.scale.x * 0.6;
          size_y = this.options.height / -this.scale.y * 0.6;
          this.culling.clear();
          this.culling.rect(-size_x / 2, -size_y / 2, size_x, size_y);
          this.culling.fill(0x333333);
          this.culling.x = t.x;
          return this.culling.y = -t.y;
        }
      }
    }

    // must be something with x and y values
    target() {
      var adjusted_position, options, position;
      options = this.level.options;
      position = this.active_object().GetPosition();
      return adjusted_position = {
        x: position.x + this.translate.x,
        y: position.y + this.translate.y + 0.25
      };
    }

    // If there are some issues on other systems than MacOS,
    // check this to find a solution : http://stackoverflow.com/questions/5527601/normalizing-mousewheel-speed-across-browsers
    init_scroll() {
      var container, scroll;
      scroll = (event) => {
        var delta, max_limit_x, max_limit_y, min_limit_x, min_limit_y;
        if (event.wheelDelta) {
          delta = event.wheelDelta / 40;
        } else if (event.detail) {
          delta = -event.detail;
        } else {
          delta = 0;
        }
        // zoom / dezoom
        this.scale.x += (this.scale.x / 200) * delta;
        this.scale.y += (this.scale.y / 200) * delta;
        // boundaries
        min_limit_x = Constants.default_scale.x / 2;
        min_limit_y = Constants.default_scale.y / 2;
        max_limit_x = Constants.default_scale.x * 2;
        max_limit_y = Constants.default_scale.y * 2;
        if (this.scale.x < min_limit_x) {
          this.scale.x = min_limit_x;
        }
        if (this.scale.y > min_limit_y) {
          this.scale.y = min_limit_y;
        }
        if (this.scale.x > max_limit_x) {
          this.scale.x = max_limit_x;
        }
        if (this.scale.y < max_limit_y) {
          this.scale.y = max_limit_y;
        }
        return event.preventDefault() && false; // defensive for old browsers
      };
      
      // Add scroll to game div container
      container = $(this.level.options.container)[0];
      container.addEventListener('DOMMouseScroll', scroll, false);
      return container.addEventListener('mousewheel', scroll, false);
    }

    compute_aabb() {
      var factor, half_h, half_w, size_x, size_y, t;
      factor = Constants.debug_culling ? 0.6 : 1.0;
      size_x = this.options.width * factor / this.scale.x;
      size_y = -this.options.height * factor / this.scale.y;
      t = this.target();
      half_w = size_x / 2;
      half_h = size_y / 2;
      this.aabb.lowerBound.Set(t.x - half_w, t.y - half_h);
      return this.aabb.upperBound.Set(t.x + half_w, t.y + half_h);
    }

  };

  b2Vec2 = Box2D.Common.Math.b2Vec2;

  Constants = (function() {
    class Constants {
      // CHAIN REACTION OF SOME ATTRIBUTES
      static chain_reaction() {
        var element, k, len, ref, results;
        // If hooking == true, no collisions !
        if (this.hooking === true) {
          ref = ['body', 'left_axle', 'right_axle', 'torso', 'lower_leg', 'upper_leg', 'lower_arm', 'upper_arm'];
          results = [];
          for (k = 0, len = ref.length; k < len; k++) {
            element = ref[k];
            results.push(Constants[element].collisions = false);
          }
          return results;
        }
      }

    };

    // GENERAL
    Constants.debug = false; // Panel on the right to debug everything

    Constants.debug_physics = false; // "Ugly" mode to debug physics

    Constants.debug_culling = false; // Debug culling of sprites

    Constants.hooking = false; // Hooking is a special trick in XMoto that allow the bike

    // to "hook" on blocks (http://www.youtube.com/watch?v=ebCgtnm_1m0)
    // (no collisions and no kill except for head)
    Constants.gravity = 9.81; // Default gravity of the game

    Constants.max_moto_speed = 70.00; // Max rotation speed of the wheels. Limit the max speed of the moto

    Constants.air_density = 0.02; // Friction of air

    Constants.moto_acceleration = 9.00; // Acceleration of moto

    Constants.biker_force = 11.00; // Force of biker when he rotates the moto

    
    // FRAMERATE
    Constants.fps = 60.0;

    // REPLAYS
    Constants.replay_key_step = 60; // Key step every x steps during replay (to beat non-deterministic behaviour)

    // See https://github.com/MichaelHoste/xmoto.js/issues/8
    Constants.replay_key_step_precision = 4; // Number of decimals when saving key step position

    
    // DISPLAY
    Constants.automatic_scale = true; // camera zoom and dezoom when moto speed changes         (can be both)

    Constants.manual_scale = true; // camera zoom and dezoom when player scrolls up and down (can be both)

    Constants.default_scale = { // default zoom of the camera
      x: 70.0,
      y: -70.0
    };

    // MOTO PARTS
    Constants.body = {
      density: 1.5,
      restitution: 0.5,
      friction: 1.0,
      position: {
        x: 0.0,
        y: 1.0
      },
      shape: [new b2Vec2(0.4, -0.3), new b2Vec2(0.50, 0.40), new b2Vec2(-0.75, 0.16), new b2Vec2(-0.35, -0.3)],
      collisions: true,
      texture: 'playerbikerbody.png',
      ghost_texture: 'ghostbikerbody.png',
      texture_size: {
        x: 2.0,
        y: 1.0
      }
    };

    Constants.left_wheel = {
      radius: 0.35,
      density: 1.8,
      restitution: 0.3,
      friction: 1.4,
      position: {
        x: -0.70,
        y: 0.48
      },
      collisions: true,
      texture: 'playerbikerwheel.png',
      ghost_texture: 'ghostbikerwheel.png'
    };

    Constants.right_wheel = {
      radius: 0.35,
      density: 1.8,
      restitution: 0.3,
      friction: 1.4,
      position: {
        x: 0.70,
        y: 0.48
      },
      collisions: true,
      texture: 'playerbikerwheel.png',
      ghost_texture: 'ghostbikerwheel.png'
    };

    Constants.left_axle = {
      density: 1.0,
      restitution: 0.5,
      friction: 1.0,
      position: {
        x: 0.0,
        y: 1.0
      },
      shape: [new b2Vec2(-0.10, -0.30), new b2Vec2(-0.25, -0.30), new b2Vec2(-0.80, -0.58), new b2Vec2(-0.65, -0.58)],
      collisions: true,
      texture: 'rear1.png',
      ghost_texture: 'rear_ghost.png'
    };

    Constants.right_axle = {
      density: 1.5,
      restitution: 0.5,
      friction: 1.0,
      position: {
        x: 0.0,
        y: 1.0
      },
      shape: [new b2Vec2(0.58, -0.02), new b2Vec2(0.48, -0.02), new b2Vec2(0.66, -0.58), new b2Vec2(0.76, -0.58)],
      collisions: true,
      texture: 'front1.png',
      ghost_texture: 'front_ghost.png'
    };

    // RIDER PARTS
    Constants.head = {
      density: 0.4,
      restitution: 0.0,
      friction: 1.0,
      position: {
        x: -0.27,
        y: 2.26
      },
      radius: 0.18,
      collisions: true
    };

    Constants.torso = {
      density: 0.4,
      restitution: 0.0,
      friction: 1.0,
      position: {
        x: -0.31,
        y: 1.89
      },
      angle: -Math.PI / 30.0,
      shape: [new b2Vec2(0.10, -0.55), new b2Vec2(0.13, 0.15), new b2Vec2(-0.20, 0.22), new b2Vec2(-0.18, -0.55)],
      collisions: true,
      texture: 'playertorso.png',
      ghost_texture: 'ghosttorso.png',
      texture_size: {
        x: 0.50,
        y: 1.20
      }
    };

    Constants.lower_leg = {
      density: 0.4,
      restitution: 0.0,
      friction: 1.0,
      position: {
        x: 0.07,
        y: 0.90
      },
      angle: -Math.PI / 6.0,
      shape: [new b2Vec2(0.2, -0.33), new b2Vec2(0.2, -0.27), new b2Vec2(0.00, -0.2), new b2Vec2(0.02, 0.33), new b2Vec2(-0.17, 0.33), new b2Vec2(-0.14, -0.33)],
      collisions: true,
      texture: 'playerlowerleg.png',
      ghost_texture: 'ghostlowerleg.png',
      texture_size: {
        x: 0.40,
        y: 0.66
      }
    };

    Constants.upper_leg = {
      density: 0.4,
      restitution: 0.0,
      friction: 1.0,
      position: {
        x: -0.15,
        y: 1.27
      },
      angle: -Math.PI / 11.0,
      shape: [new b2Vec2(0.4, -0.14), new b2Vec2(0.4, 0.07), new b2Vec2(-0.4, 0.14), new b2Vec2(-0.4, -0.08)],
      collisions: true,
      texture: 'playerupperleg.png',
      ghost_texture: 'ghostupperleg.png',
      texture_size: {
        x: 0.78,
        y: 0.28
      }
    };

    Constants.lower_arm = {
      density: 0.4,
      restitution: 0.0,
      friction: 1.0,
      position: {
        x: 0.07,
        y: 1.54
      },
      angle: -Math.PI / 10.0,
      shape: [new b2Vec2(0.28, -0.07), new b2Vec2(0.28, 0.04), new b2Vec2(-0.30, 0.07), new b2Vec2(-0.30, -0.06)],
      collisions: true,
      texture: 'playerlowerarm.png',
      ghost_texture: 'ghostlowerarm.png',
      texture_size: {
        x: 0.53,
        y: 0.20
      }
    };

    Constants.upper_arm = {
      density: 0.4,
      restitution: 0.0,
      friction: 1.0,
      position: {
        x: -0.20,
        y: 1.85
      },
      angle: Math.PI / 10.0,
      shape: [new b2Vec2(0.09, -0.29), new b2Vec2(0.09, 0.22), new b2Vec2(-0.11, 0.26), new b2Vec2(-0.10, -0.29)],
      collisions: true,
      texture: 'playerupperarm.png',
      ghost_texture: 'ghostupperarm.png',
      texture_size: {
        x: 0.24,
        y: 0.56
      }
    };

    // MOTO JOINTS
    Constants.left_suspension = {
      angle: new b2Vec2(0, 1),
      lower_translation: -0.03,
      upper_translation: 0.20,
      back_force: 3.00,
      rigidity: 8.00
    };

    Constants.right_suspension = {
      angle: new b2Vec2(-0.2, 1),
      lower_translation: -0.01,
      upper_translation: 0.20,
      back_force: 3.00,
      rigidity: 4.00
    };

    // RIDER JOINTS
    Constants.ankle = {
      axe_position: {
        x: -0.18,
        y: -0.2
      }
    };

    Constants.wrist = {
      axe_position: {
        x: 0.25,
        y: -0.07
      }
    };

    Constants.knee = {
      axe_position: {
        x: 0.12,
        y: 0.28
      }
    };

    Constants.elbow = {
      axe_position: {
        x: 0.03,
        y: -0.21
      }
    };

    Constants.shoulder = {
      axe_position: {
        x: -0.12,
        y: 0.22
      }
    };

    Constants.hip = {
      axe_position: {
        x: -0.25,
        y: 0.14
      }
    };

    // GROUND
    Constants.ground = {
      density: 1.0,
      restitution: 0.2,
      friction: 1.2
    };

    Constants.chain_reaction();

    return Constants;

  }).call(this);

  Input = class Input {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
      this.up = false;
      this.down = false;
      this.left = false;
      this.right = false;
      this.space = false;
    }

    init() {
      this.disable_scroll();
      return this.init_keyboard();
    }

    disable_scroll() {
      var keydown, keys, preventDefault;
      // Disable up, down, left, right to scroll
      // left: 37, up: 38, right: 39, down: 40, spacebar: 32, pageup: 33, pagedown: 34, end: 35, home: 36
      keys = [37, 38, 39, 40, 32];
      preventDefault = function(e) {
        e = e || window.event;
        if (e.preventDefault) {
          return e.preventDefault();
        } else {
          return e.returnValue = false;
        }
      };
      keydown = function(e) {
        var i, k, len;
        for (k = 0, len = keys.length; k < len; k++) {
          i = keys[k];
          if (e.keyCode === i) {
            preventDefault(e);
            return;
          }
        }
      };
      return document.onkeydown = keydown;
    }

    init_keyboard() {
      $(document).off('keydown');
      $(document).on('keydown', (event) => {
        var canvas_selector, image, url;
        switch (event.which || event.keyCode) {
          case 38:
            return this.up = true;
          case 40:
            return this.down = true;
          case 37:
            return this.left = true;
          case 39:
            return this.right = true;
          case 32:
            return this.space = true;
          case 13: // enter
            return this.level.need_to_restart = true;
          case 70: // f
            if (!$(':input').is(':focus')) {
              if (!event.ctrlKey && !event.metaKey && !event.altKey) {
                this.level.toggle_fullscreen();
                return event.preventDefault();
              }
            }
            break;
          case 69: // e
            if (!$(':input').is(':focus')) {
              if (!event.ctrlKey && !event.metaKey && !event.altKey) {
                return this.level.moto.rider.eject();
              }
            }
            break;
          case 67: // c
            url = document.URL;
            url = url.substr(url.length - 1) !== '/' ? `${url}/capture` : `${url}capture`;
            canvas_selector = `${this.level.options.container} > canvas:first`;
            image = $(canvas_selector)[0].toDataURL();
            return $.post(url, {
              steps: this.level.physics.steps,
              image: image
            }).done(function() {
              return alert("Capture uploaded");
            }).fail(function() {
              return alert("Capture failed");
            });
        }
      });
      return $(document).on('keyup', (event) => {
        switch (event.which || event.keyCode) {
          case 38:
            return this.up = false;
          case 40:
            return this.down = false;
          case 37:
            return this.left = false;
          case 39:
            return this.right = false;
        }
      });
    }

  };

  Level = class Level {
    constructor(renderer, options) {
      this.renderer = renderer;
      this.options = options;
      // Context
      this.debug_ctx = $('#xmoto-debug')[0].getContext('2d');
      this.stage = new PIXI.Container();
      this.stage.label = this.options.container; // id of div
      
      // Debug
      this.init_debug_physics();
      this.init_devtools();
      // Level independant objects
      this.assets = new Assets();
      this.camera = new Camera(this);
      this.physics = new Physics(this);
      this.input = new Input(this);
      this.listeners = new Listeners(this);
      this.moto = new Moto(this);
      this.particles = new Particles(this);
      // Level dependent objects
      this.infos = new Infos(this);
      this.replacements = new Replacements(this);
      this.layers = new Layers(this);
      this.sky = new Sky(this);
      this.blocks = new Blocks(this);
      this.limits = new Limits(this);
      this.script = new Script(this);
      this.entities = new Entities(this);
      // Replay: actual run of the player (not saved yet)
      this.replay = new Replay(this);
      // Ghosts: previous saved run of various players (included himself)
      this.ghosts = new Ghosts(this);
      // Manage fullscreen on/off
      this.bind_fullscreen();
    }

    load_from_file(filename, callback) {
      return this.assets.parse_theme(() => {
        return $.ajax({
          type: "GET",
          url: `${this.options.levels_path}/${filename}`,
          dataType: "xml",
          success: function(xml) {
            return this.load_level(xml, callback);
          },
          context: this
        });
      });
    }

    load_level(xml, callback) {
      this.infos.parse(xml);
      this.replacements.parse(xml);
      this.sky.parse(xml);
      this.layers.parse(xml);
      this.blocks.parse(xml);
      this.limits.parse(xml);
      this.script.parse(xml);
      this.entities.parse(xml);
      this.sky.load_assets();
      this.blocks.load_assets();
      this.limits.load_assets();
      this.entities.load_assets();
      this.moto.load_assets();
      this.ghosts.load_assets();
      return this.assets.load(callback);
    }

    init() {
      this.sky.init();
      this.layers.init();
      this.blocks.init();
      this.limits.init();
      this.entities.init();
      this.moto.init();
      this.ghosts.init();
      this.physics.init();
      this.input.init();
      this.camera.init();
      this.listeners.init();
      return this.init_timer();
    }

    update() {
      var live_player, live_replay;
      this.physics.update();
      live_player = this.options.playable && !this.moto.dead;
      live_replay = !this.options.playable && !this.ghosts.player.moto.dead;
      if (live_player || live_replay) {
        this.update_timer();
      }
      this.sky.update();
      this.layers.update();
      this.limits.update();
      this.entities.update();
      this.camera.update();
      this.blocks.update();
      if (this.options.playable) {
        this.moto.update();
      }
      this.ghosts.update();
      return this.particles.update();
    }

    init_timer() {
      return this.start_time = new Date().getTime(); // in ms
    }

    update_timer() {
      var cents, elapsed, minutes, new_time, seconds, text;
      new_time = new Date().getTime() - this.start_time;
      elapsed = Math.floor(new_time / 10);
      minutes = Math.floor(elapsed / 6000);
      seconds = Math.floor(elapsed / 100) % 60;
      cents = elapsed % 100;
      text = `${this.pad(seconds)}:${this.pad(cents)}`;
      if (minutes > 0) { // show minutes only if needed
        text = `${minutes}:${text}`;
      }
      return $(this.options.chrono).text(text);
    }

    pad(n) {
      return String(n).padStart(2, '0');
    }

    got_strawberries() {
      var k, len, ref, strawberry;
      ref = this.entities.strawberries;
      for (k = 0, len = ref.length; k < len; k++) {
        strawberry = ref[k];
        if (strawberry.display) {
          return false;
        }
      }
      return true;
    }

    respawn_strawberries() {
      var entity, k, len, ref, results;
      ref = this.entities.strawberries;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        entity = ref[k];
        results.push(entity.display = true);
      }
      return results;
    }

    restart() {
      this.replay = new Replay(this);
      this.ghosts.reload();
      this.moto.destroy();
      this.moto = new Moto(this);
      this.moto.init();
      this.respawn_strawberries();
      this.init_timer();
      return this.update_timer();
    }

    init_debug_physics() {
      if (Constants.debug_physics) {
        $(`${this.options.container} > canvas`).hide();
        return $('#xmoto-debug').show();
      }
    }

    // To debug the stage in Chrome: https://pixijs.io/devtools/
    init_devtools() {
      return window.__PIXI_DEVTOOLS__ = {
        stage: this.stage,
        renderer: this.renderer
      };
    }

    toggle_fullscreen() {
      if (document.fullscreenElement) {
        return document.exitFullscreen();
      } else {
        return $(this.options.container)[0].requestFullscreen();
      }
    }

    bind_fullscreen() {
      return $(document).on('fullscreenchange webkitfullscreenchange mozfullscreenchange MSFullscreenChange', () => {
        var debug_canvas, new_scale_x, new_scale_y, ratio;
        debug_canvas = $('#xmoto-debug')[0];
        if (document.fullscreenElement) {
          this.original_width = this.options.width;
          this.original_height = this.options.height;
          this.original_scale_x = Constants.default_scale.x;
          this.original_scale_y = Constants.default_scale.y;
          this.renderer.resize(screen.width, screen.height);
          this.options.width = screen.width;
          this.options.height = screen.height;
          if (debug_canvas) {
            debug_canvas.width = screen.width;
            debug_canvas.height = screen.height;
            this.debug_ctx.lineWidth = 0.03; // must be redefined
          }
          ratio = Math.min(screen.width / this.original_width, screen.height / this.original_height);
          new_scale_x = this.original_scale_x * ratio;
          new_scale_y = this.original_scale_y * ratio;
          Constants.default_scale = {
            x: new_scale_x,
            y: new_scale_y
          };
          this.camera.scale.x = new_scale_x;
          return this.camera.scale.y = new_scale_y;
        } else {
          this.renderer.resize(this.original_width, this.original_height);
          this.options.width = this.original_width;
          this.options.height = this.original_height;
          if (debug_canvas) {
            debug_canvas.width = this.original_width;
            debug_canvas.height = this.original_height;
            this.debug_ctx.lineWidth = 0.03; // must be redefined
          }
          Constants.default_scale = {
            x: this.original_scale_x,
            y: this.original_scale_y
          };
          this.camera.scale.x = this.original_scale_x;
          return this.camera.scale.y = this.original_scale_y;
        }
      });
    }

  };

  Listeners = class Listeners {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
      this.world = level.physics.world;
    }

    active_moto() {
      if (this.level.options.playable) {
        return this.level.moto;
      } else {
        return this.level.ghosts.player.moto;
      }
    }

    init() {
      var listener;
      // Add listeners for end of level
      listener = new Box2D.Dynamics.b2ContactListener();
      listener.BeginContact = (contact) => {
        var a, b, entity, moto, strawberry;
        moto = this.active_moto();
        a = contact.GetFixtureA().GetBody().GetUserData();
        b = contact.GetFixtureB().GetBody().GetUserData();
        if (!moto.dead) {
          // Strawberries
          if (Listeners.does_contact_moto_rider(a, b, 'strawberry')) {
            strawberry = a.name === 'strawberry' ? contact.GetFixtureA() : contact.GetFixtureB();
            entity = strawberry.GetBody().GetUserData().entity;
            if (entity.display) {
              return entity.display = false;
            }
          //createjs.Sound.play('PickUpStrawberry')

          // End of level
          } else if (Listeners.does_contact_moto_rider(a, b, 'end_of_level') && !this.level.need_to_restart) {
            if (this.level.got_strawberries()) {
              if (a.name === 'rider' || b.name === 'rider') {
                moto = a.name === 'rider' ? a.rider.moto : b.rider.moto;
              } else {
                moto = a.name === 'moto' ? a.moto : b.moto;
              }
              return this.trigger_restart(moto);
            }
          // Fall of rider
          } else if (Constants.hooking === false && Listeners.does_contact(a, b, 'rider', 'ground') && a.part !== 'lower_leg' && b.part !== 'lower_leg') {
            moto = a.name === 'rider' ? a.rider.moto : b.rider.moto;
            return this.kill_moto(moto);
          } else if (Constants.hooking === true && Listeners.does_contact(a, b, 'rider', 'ground') && (a.part === 'head' || b.part === 'head')) {
            moto = a.name === 'rider' ? a.rider.moto : b.rider.moto;
            return this.kill_moto(moto);
          // Wrecker contact
          } else if (Listeners.does_contact_moto_rider(a, b, 'wrecker')) {
            if (a.name === 'rider' || b.name === 'rider') {
              moto = a.name === 'rider' ? a.rider.moto : b.rider.moto;
            } else {
              moto = a.name === 'moto' ? a.moto : b.moto;
            }
            return this.kill_moto(moto);
          }
        }
      };
      return this.world.SetContactListener(listener);
    }

    static does_contact_moto_rider(a, b, obj) {
      var collision, player;
      collision = Listeners.does_contact(a, b, obj, 'rider') || Listeners.does_contact(a, b, obj, 'moto');
      player = a.type === 'player' || b.type === 'player';
      return collision && player;
    }

    static does_contact(a, b, obj1, obj2) {
      return (a.name === obj1 && b.name === obj2) || (a.name === obj2 && b.name === obj1);
    }

    trigger_restart(moto) {
      //createjs.Sound.play('EndOfLevel')
      if (moto.ghost) {
        return moto.dead = true;
      } else {
        this.level.replay.success = true;
        return this.level.need_to_restart = true;
      }
    }

    kill_moto(moto) {
      if (!moto.dead) {
        moto.dead = true;
        // Cause the game to "hard" crash because reactivation of collisions when in the middle of it
        //@level.moto.rider.torso.GetFixtureList().SetSensor(false)
        //@level.moto.rider.lower_leg.GetFixtureList().SetSensor(false)
        //@level.moto.rider.upper_leg.GetFixtureList().SetSensor(false)
        //@level.moto.rider.lower_arm.GetFixtureList().SetSensor(false)
        //@level.moto.rider.upper_arm.GetFixtureList().SetSensor(false)
        //@level.moto.body.GetFixtureList().SetSensor(false)
        //@level.moto.left_axle.GetFixtureList().SetSensor(false)
        //@level.moto.right_axle.GetFixtureList().SetSensor(false)

        //createjs.Sound.play('Headcrash')
        this.world.DestroyJoint(moto.rider.ankle_joint);
        this.world.DestroyJoint(moto.rider.wrist_joint);
        moto.rider.shoulder_joint.m_enableLimit = false;
        moto.rider.knee_joint.m_lowerAngle = moto.rider.knee_joint.m_lowerAngle * 3;
        moto.rider.elbow_joint.m_upperAngle = moto.rider.elbow_joint.m_upperAngle * 3;
        return moto.rider.hip_joint.m_lowerAngle = moto.rider.hip_joint.m_lowerAngle * 3;
      }
    }

  };

  $.xmoto = function(level_filename, options = {}) {
    var bind_render_to_dom, bind_stats_fps, bind_stats_ms, initialize, load_options, main_loop;
    initialize = async function() {
      var renderer;
      options = load_options(options);
      renderer = (await PIXI.autoDetectRenderer({
        preference: 'webgl', // or 'webgpu' or 'canvas'
        width: options.width,
        height: options.height,
        background: 0xFFFFFF,
        clearBeforeRender: false, // No need to clear, we paint the entire canvas
        textureGCActive: false // We manage texture GC manually (`renderer.gc.enabled` can be toggled)
      }));
      //antialias:             true  # Default to "false" for performance, but it doesn't seem to impact a lot, and way better rendering! (disable if needed)
      //preserveDrawingBuffer: true  # Need to be true when capturing with "toDataUrl" (may have low performance impact)
      //transparent: true            # May be useful later (moto on website)
      window.cancelAnimationFrame(window.game_loop);
      bind_render_to_dom(renderer, options);
      return main_loop(level_filename, renderer, options);
    };
    load_options = function(options) {
      var defaults;
      defaults = {
        // Selectors
        container: '#xmoto', // empty div where the game will be created
        loading: '#loading', // loading selector
        chrono: '#chrono', // chrono selector
        
        // Size
        width: 800,
        height: 600,
        // Replays
        replays: [], // [ { replay: , follow: , name: , picture: }, ... ]
        playable: true, // if false, just watch replays
        
        // Zoom
        zoom: Constants.default_scale.x, // Zoom of camera
        
        // Paths
        levels_path: '/data/Levels', // Path where are the levels (ex. /data/Levels/l1.lvl)
        scores_path: '/scores', // Path where to POST a score
        replays_path: '/data/Replays' // Path where all the replay files are stored (ex. /data/Replays/1.replay)
      };
      options = $.extend(defaults, options);
      Constants.default_scale = {
        x: options.zoom,
        y: -options.zoom
      };
      return options;
    };
    bind_render_to_dom = function(renderer, options) {
      var container, debug_canvas_html;
      container = $(options.container);
      container.find('canvas').remove(); // Remove old canvas
      $(options.loading).show(); // Start loading
      container.css('height', options.height); // Force height of parent
      container[0].appendChild(renderer.canvas); // Add PixiJS canvas to container
      debug_canvas_html = '<canvas id="xmoto-debug" width="' + options.width + '" height="' + options.height + '"></canvas>';
      container.append(debug_canvas_html); // Add debug canvas to container (physics)
      return $('#xmoto-debug').hide(); // Hide debug canvas
    };
    bind_stats_fps = function(container_selector) {
      var stats;
      stats = new Stats();
      stats.showPanel(0); // 0: fps, 1: ms, 2: mb, 3+: custom
      $(container_selector)[0].appendChild(stats.dom);
      $(container_selector).find('div:last').addClass('stats-fps');
      return stats;
    };
    bind_stats_ms = function(container_selector) {
      var stats;
      stats = new Stats();
      stats.showPanel(1); // 0: fps, 1: ms, 2: mb, 3+: custom
      $(container_selector)[0].appendChild(stats.dom);
      $(container_selector).find('div:last').addClass('stats-ms');
      return stats;
    };
    main_loop = function(level_filename, renderer, options) {
      var container_selector, level, stats_fps, stats_ms;
      container_selector = $(options.container);
      if (Constants.debug) {
        stats_fps = bind_stats_fps(container_selector);
      }
      if (Constants.debug) {
        stats_ms = bind_stats_ms(container_selector);
      }
      level = new Level(renderer, options);
      return level.load_from_file(level_filename, () => {
        var update;
        level.init(renderer);
        $(options.loading).hide();
        update = () => {
          if (Constants.debug) {
            stats_fps.begin();
          }
          if (Constants.debug) {
            stats_ms.begin();
          }
          level.update();
          if (!Constants.debug_physics) {
            renderer.render(level.stage, {
              clear: false
            });
          }
          window.game_loop = requestAnimationFrame(update);
          if (Constants.debug) {
            stats_fps.end();
          }
          if (Constants.debug) {
            return stats_ms.end();
          }
        };
        return update();
      });
    };
    return initialize();
  };

  b2World = Box2D.Dynamics.b2World;

  b2Vec2 = Box2D.Common.Math.b2Vec2;

  b2AABB = Box2D.Collision.b2AABB;

  b2BodyDef = Box2D.Dynamics.b2BodyDef;

  b2Body = Box2D.Dynamics.b2Body;

  b2FixtureDef = Box2D.Dynamics.b2FixtureDef;

  b2Fixture = Box2D.Dynamics.b2Fixture;

  b2MassData = Box2D.Collision.Shapes.b2MassData;

  b2PolygonShape = Box2D.Collision.Shapes.b2PolygonShape;

  b2CircleShape = Box2D.Collision.Shapes.b2CircleShape;

  b2EdgeShape = Box2D.Collision.Shapes.b2EdgeShape;

  b2EdgeChainDef = Box2D.Collision.Shapes.b2EdgeChainDef;

  b2DebugDraw = Box2D.Dynamics.b2DebugDraw;

  b2MouseJointDef = Box2D.Dynamics.Joints.b2MouseJointDef;

  b2Settings = Box2D.Common.b2Settings;

  Physics = class Physics {
    constructor(level) {
      var debugDraw;
      this.level = level;
      this.options = level.options;
      this.camera = level.camera;
      this.world = new b2World(new b2Vec2(0, -Constants.gravity), true); // gravity vector, and doSleep
      this.debug_ctx = level.debug_ctx;
      // Double default precision between wheel and ground
      b2Settings.b2_linearSlop = 0.0025;
      // debug initialization
      debugDraw = new b2DebugDraw();
      debugDraw.SetSprite(this.debug_ctx); // context
      this.debug_ctx.lineWidth = 0.03; // thickness of line (debugDraw.SetLineThickness doesn't work)
      debugDraw.SetFillAlpha(0.5); // transparency
      debugDraw.m_sprite.graphics.clear = function() {};
      debugDraw.SetFlags(b2DebugDraw.e_shapeBit | b2DebugDraw.e_jointBit);
      this.world.SetDebugDraw(debugDraw);
      this.world;
    }

    init() {
      this.last_step = new Date().getTime();
      this.step = 1000.0 / Constants.fps;
      return this.steps = 0;
    }

    restart() {
      var player_ghost, replay, time;
      replay = this.level.replay;
      player_ghost = this.level.ghosts.player;
      // save replay if better (local + server)
      if (replay.success) {
        time = (replay.steps / 60.0).toFixed(2).replace('.', ':');
        if (!player_ghost || player_ghost.replay.steps > replay.steps) {
          this.save_replay_and_init_ghosts(replay);
          console.log(`WIN : you improved your personal score : ${time} (${replay.steps} steps)`);
        } else {
          console.log(`FAIL : you didn't improve your personal score : ${time} (${replay.steps} steps)`);
        }
      }
      this.level.restart();
      return this.init();
    }

    save_replay_and_init_ghosts(replay) {
      replay.add_step(); // add last step
      replay.save();
      // Update replay of player ghost, or create new one
      if (this.level.ghosts.player) {
        this.level.ghosts.player.replay = replay.clone();
        return this.level.ghosts.player.reload();
      } else {
        this.level.ghosts.player = new Ghost(this.level, replay.clone());
        return this.level.ghosts.player.init();
      }
    }

    update() {
      var results;
      results = [];
      while ((new Date()).getTime() - this.last_step > this.step) {
        this.steps = this.steps + 1;
        this.last_step += this.step;
        this.level.moto.move();
        this.level.ghosts.move();
        this.level.replay.add_step();
        this.level.camera.move();
        this.world.Step(1.0 / Constants.fps, 10, 10);
        this.world.ClearForces();
        this.level.input.space = false; // Space can't stay pressed (used for `.move` and `.add_step`)
        if (this.level.need_to_restart) {
          this.restart();
          results.push(this.level.need_to_restart = false);
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    create_polygon(vertices, name, density = 1.0, restitution = 0.5, friction = 1.0, group_index = -2) {
      var bodyDef, fixDef;
      // Create fixture
      fixDef = new b2FixtureDef();
      fixDef.shape = new b2PolygonShape();
      fixDef.density = density;
      fixDef.restitution = restitution;
      fixDef.friction = friction;
      fixDef.filter.groupIndex = group_index;
      // Create polygon
      Physics.create_shape(fixDef, vertices);
      // Create body
      bodyDef = new b2BodyDef();
      // Assign body position
      bodyDef.position.x = 0;
      bodyDef.position.y = 0;
      bodyDef.userData = {
        name: name
      };
      bodyDef.type = b2Body.b2_staticBody;
      // Assign fixture to body and add body to 2D world
      return this.world.CreateBody(bodyDef).CreateFixture(fixDef);
    }

    create_lines(block, name, density = 1.0, restitution = 0.5, friction = 1.0, group_index = -2) {
      var body, bodyDef, fixDef, i, k, len, ref, results, vertex, vertex1, vertex2;
      // Create body
      bodyDef = new b2BodyDef();
      // Assign body position
      bodyDef.position.x = block.position.x;
      bodyDef.position.y = block.position.y;
      bodyDef.userData = {
        name: name
      };
      bodyDef.type = b2Body.b2_staticBody;
      // add body to the world
      body = this.world.CreateBody(bodyDef);
      ref = block.vertices;
      // assign each couple of vertices to a line
      results = [];
      for (i = k = 0, len = ref.length; k < len; i = ++k) {
        vertex = ref[i];
        // Create fixture
        fixDef = new b2FixtureDef();
        fixDef.shape = new b2PolygonShape();
        fixDef.density = density;
        fixDef.restitution = restitution;
        fixDef.friction = friction;
        fixDef.filter.groupIndex = group_index;
        // Create line (from polygon because box2Dweb cannot do otherwise)
        vertex1 = vertex;
        vertex2 = i === block.vertices.length - 1 ? block.vertices[0] : block.vertices[i + 1];
        fixDef.shape.SetAsArray([new b2Vec2(vertex1.x, vertex1.y), new b2Vec2(vertex2.x, vertex2.y)], 2);
        // Assign fixture (line) to body
        results.push(body.CreateFixture(fixDef));
      }
      return results;
    }

    static create_shape(fix_def, shape, mirror = false) {
      var b2vertices, k, l, len, len1, vertex;
      b2vertices = [];
      if (mirror === false) {
        for (k = 0, len = shape.length; k < len; k++) {
          vertex = shape[k];
          b2vertices.push(new b2Vec2(vertex.x, vertex.y));
        }
      } else {
        for (l = 0, len1 = shape.length; l < len1; l++) {
          vertex = shape[l];
          b2vertices.unshift(new b2Vec2(-vertex.x, vertex.y));
        }
      }
      return fix_def.shape.SetAsArray(b2vertices);
    }

  };

  Blocks = class Blocks {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
      this.list = [];
    }

    parse(xml) {
      var block, i, k, l, len, len1, len2, m, material, texture_params, vertex, xml_block, xml_blocks, xml_material, xml_materials, xml_vertex, xml_vertices;
      xml_blocks = $(xml).find('block');
      for (k = 0, len = xml_blocks.length; k < len; k++) {
        xml_block = xml_blocks[k];
        block = {
          id: $(xml_block).attr('id'),
          position: {
            x: parseFloat($(xml_block).find('position').attr('x')),
            y: parseFloat($(xml_block).find('position').attr('y')),
            dynamic: $(xml_block).find('position').attr('dynamic') === 'true',
            background: $(xml_block).find('position').attr('background') === 'true',
            islayer: $(xml_block).find('position').attr('islayer') === 'true',
            layerid: (function() {
              var attr;
              attr = $(xml_block).find('position').attr('layerid');
              if (attr != null) {
                return parseInt(attr);
              } else {
                return void 0; // integer or undefined if no attribute
              }
            })()
          },
          usetexture: {
            id: $(xml_block).find('usetexture').attr('id').toLowerCase(),
            scale: parseFloat($(xml_block).find('usetexture').attr('scale')) || 1.0
          },
          physics: {
            grip: parseFloat($(xml_block).find('physics').attr('grip'))
          },
          edges: {
            angle: parseFloat($(xml_block).find('edges').attr('angle')),
            materials: []
          },
          vertices: []
        };
        block.no_collision = block.position.background; // background                   => no collision (old syntax)
        block.no_collision || (block.no_collision = block.position.islayer && block.position.layerid !== void 0); // layer with specific layerid  => no collision
        if (block.usetexture.id === 'default') {
          block.usetexture.id = 'dirt';
        }
        texture_params = this.assets.theme.texture_params(block.usetexture.id);
        if (texture_params.frames > 0) {
          block.animated = true;
          block.frames_count = texture_params.frames;
          block.delay = texture_params.delay;
          block.frame_names = (function() {
            var l, ref, results;
            results = [];
            for (i = l = 0, ref = texture_params.frames - 1; (0 <= ref ? l <= ref : l >= ref); i = 0 <= ref ? ++l : --l) {
              results.push(this.frame_name(texture_params, i));
            }
            return results;
          }).call(this);
          block.texture_name = block.frame_names[0];
        } else {
          block.animated = false;
          block.texture_name = texture_params.file;
        }
        xml_materials = $(xml_block).find('edges material');
        for (l = 0, len1 = xml_materials.length; l < len1; l++) {
          xml_material = xml_materials[l];
          material = {
            name: $(xml_material).attr('name'),
            edge: $(xml_material).attr('edge'),
            color_r: parseInt($(xml_material).attr('color_r')),
            color_g: parseInt($(xml_material).attr('color_g')),
            color_b: parseInt($(xml_material).attr('color_b')),
            color_a: parseInt($(xml_material).attr('color_a')),
            scale: parseFloat($(xml_material).attr('scale')),
            depth: parseFloat($(xml_material).attr('depth'))
          };
          block.edges.materials.push(material);
        }
        xml_vertices = $(xml_block).find('vertex');
        for (m = 0, len2 = xml_vertices.length; m < len2; m++) {
          xml_vertex = xml_vertices[m];
          vertex = {
            x: parseFloat($(xml_vertex).attr('x')),
            y: parseFloat($(xml_vertex).attr('y')),
            absolute_x: parseFloat($(xml_vertex).attr('x')) + block.position.x, // absolutes positions are practical
            absolute_y: parseFloat($(xml_vertex).attr('y')) + block.position.y, // for edges creation
            edge: $(xml_vertex).attr('edge') ? $(xml_vertex).attr('edge').toLowerCase() : void 0
          };
          block.vertices.push(vertex);
        }
        block.edges_list = new Edges(this.level, block);
        //block.edges_list = new EdgesWithAngle(@level, block)
        block.edges_list.parse();
        block.aabb = this.compute_aabb(block);
        this.list.push(block);
      }
      this.list.sort(this.sort_blocks_by_texture);
      return this;
    }

    load_assets() {
      var block, frame_name, k, l, len, len1, ref, ref1, results;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        block = ref[k];
        if (block.animated) {
          ref1 = block.frame_names;
          for (l = 0, len1 = ref1.length; l < len1; l++) {
            frame_name = ref1[l];
            this.assets.textures.push(frame_name);
          }
        } else {
          this.assets.textures.push(block.texture_name);
        }
        results.push(block.edges_list.load_assets());
      }
      return results;
    }

    init() {
      var block, k, len, ref, results;
      this.init_physics();
      this.init_sprites();
      this.init_culling_debug();
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        block = ref[k];
        results.push(block.edges_list.init());
      }
      return results;
    }

    init_physics() {
      var block, ground, k, len, ref, results;
      ground = Constants.ground;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        block = ref[k];
        if (!block.no_collision) {
          results.push(this.level.physics.create_lines(block, 'ground', ground.density, ground.restitution, ground.friction));
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    init_sprites() {
      var block, k, len, name, ref, results;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        block = ref[k];
        // Build polygon in world PIXI coordinates (y inverted)
        block.points = block.vertices.map(function(v) {
          return new PIXI.Point(v.absolute_x, -v.absolute_y);
        });
        // Not translating the matrix ensures texture continuity between blocks
        block.matrix = new PIXI.Matrix();
        block.matrix.scale(block.usetexture.scale / 60.0, -block.usetexture.scale / 60.0);
        //block.matrix.translate(block.aabb.lowerBound.x, -block.aabb.upperBound.y)
        if (block.animated) {
          block.frame_textures = (function() {
            var l, len1, ref1, results1;
            ref1 = block.frame_names;
            results1 = [];
            for (l = 0, len1 = ref1.length; l < len1; l++) {
              name = ref1[l];
              results1.push(PIXI.Texture.from(this.assets.get_url(name)));
            }
            return results1;
          }).call(this);
          block.current_frame = 0;
          // Stagger the animation start so neighbouring blocks of the same texture
          // don't tear: they all share the same wall-clock based frame index.
          block.animation_start = performance.now();
        } else {
          block.texture = PIXI.Texture.from(this.assets.get_url(block.texture_name));
        }
        // Create graphics with texture and matrix
        block.graphics = new PIXI.Graphics();
        block.graphics.label = block.id;
        this.draw_block_fill(block, block.animated ? block.frame_textures[0] : block.texture);
        results.push(this.level.layers.layer_for_block(block).addChild(block.graphics));
      }
      return results;
    }

    draw_block_fill(block, texture) {
      block.graphics.clear();
      block.graphics.poly(block.points);
      return block.graphics.fill({
        texture: texture,
        matrix: block.matrix,
        color: 0xffffff,
        alpha: 1.0,
        textureSpace: 'global'
      });
    }

    // One Graphic is created in each block layer (parallax or static)
    // for drawing culling rectangles. It's because parallax layers
    // have their own scale/translate reference
    init_culling_debug() {
      var block, graphics, k, layer, len, ref, results;
      if (Constants.debug_culling) {
        this.culling_debug_graphics = {}; // { label => graphics }
        ref = this.list;
        results = [];
        for (k = 0, len = ref.length; k < len; k++) {
          block = ref[k];
          layer = this.level.layers.layer_for_block(block);
          if (!this.culling_debug_graphics[layer.label]) {
            graphics = new PIXI.Graphics();
            graphics.label = `culling (blocks - ${layer.label})`;
            graphics.parallax = layer.parallax; // needed for later
            layer.addChild(graphics);
            results.push(this.culling_debug_graphics[layer.label] = graphics);
          } else {
            results.push(void 0);
          }
        }
        return results;
      }
    }

    update() {
      var block, k, len, now, ref;
      if (!Constants.debug_physics) {
        now = performance.now();
        ref = this.list;
        for (k = 0, len = ref.length; k < len; k++) {
          block = ref[k];
          block.graphics.visible = this.visible(block);
          block.edges_list.update();
          if (block.animated && block.graphics.visible) {
            this.update_animation(block, now);
          }
        }
      }
      if (Constants.debug_culling) {
        return this.draw_debug_culling();
      }
    }

    update_animation(block, now) {
      var elapsed, frame;
      elapsed = (now - block.animation_start) / 1000.0;
      frame = Math.floor(elapsed / block.delay) % block.frames_count;
      if (frame !== block.current_frame) {
        block.current_frame = frame;
        return this.draw_block_fill(block, block.frame_textures[frame]);
      }
    }

    draw_debug_culling() {
      var block, calling_debug, culling_debug, culling_debugs, k, l, layer, len, len1, len2, line_width, m, ref, ref1, results;
      culling_debugs = Object.values(this.culling_debug_graphics);
      for (k = 0, len = culling_debugs.length; k < len; k++) {
        culling_debug = culling_debugs[k];
        culling_debug.clear();
      }
      ref = this.list;
      for (l = 0, len1 = ref.length; l < len1; l++) {
        block = ref[l];
        if (block.aabb && block.graphics.visible) {
          layer = this.level.layers.layer_for_block(block);
          culling_debug = this.culling_debug_graphics[layer.label];
          culling_debug.rect(block.aabb.lowerBound.x, -block.aabb.upperBound.y, block.aabb.upperBound.x - block.aabb.lowerBound.x, block.aabb.upperBound.y - block.aabb.lowerBound.y);
        }
      }
      ref1 = Object.values(this.culling_debug_graphics);
      // Parallax layers use a fixed 1px stroke (pixelLine) to stay readable
      results = [];
      for (m = 0, len2 = ref1.length; m < len2; m++) {
        calling_debug = ref1[m];
        if (calling_debug.parallax) {
          results.push(calling_debug.stroke({
            color: 0xC7C778,
            alpha: 0.7,
            pixelLine: true
          }));
        } else {
          line_width = 0.04 * Constants.default_scale.x / this.level.camera.scale.x;
          results.push(calling_debug.stroke({
            width: line_width,
            color: 0xC7C778,
            alpha: 0.7
          }));
        }
      }
      return results;
    }

    visible(block) {
      var parallax_layer;
      if (block.position.islayer && block.position.layerid !== void 0) {
        parallax_layer = this.level.layers.list[block.position.layerid];
        return block.aabb.TestOverlap(parallax_layer.camera_aabb);
      } else {
        return block.aabb.TestOverlap(this.level.camera.aabb);
      }
    }

    compute_aabb(block) {
      var aabb, x_positions, y_positions;
      x_positions = block.vertices.map(function(v) {
        return v.absolute_x;
      });
      y_positions = block.vertices.map(function(v) {
        return v.absolute_y;
      });
      aabb = new Box2D.Collision.b2AABB();
      aabb.lowerBound.Set(Math.min.apply(null, x_positions), Math.min.apply(null, y_positions));
      aabb.upperBound.Set(Math.max.apply(null, x_positions), Math.max.apply(null, y_positions));
      return aabb;
    }

    frame_name(texture_params, frame_number) {
      return `${texture_params.file_base}${(frame_number / 100.0).toFixed(2).toString().substring(2)}.${texture_params.file_extension}`;
    }

    // Blocks drawing is sorted by textures:
    // http://wiki.xmoto.tuxfamily.org/index.php?title=Others_tips_to_make_levels#Parallax_layers
    sort_blocks_by_texture(a, b) {
      if (a.usetexture.id > b.usetexture.id) {
        return 1;
      }
      if (a.usetexture.id <= b.usetexture.id) {
        return -1;
      }
      return 0;
    }

  };

  // Old "naïve" Edge method. Drop-in replacement but:
  //  -> 1 rectangle every 2 vertices + depth
  //  -> Texture with same angle as rectangle
  //  -> Visually quite nice but not the native XMoto implementation, and too many GPU calls?
  EdgesWithAngle = class EdgesWithAngle {
    constructor(level, block) {
      this.level = level;
      this.block = block;
      this.assets = this.level.assets;
      this.list = []; // List of edges
    }

    parse() {
      var edge, i, k, len, ref, results, vertex;
      ref = this.block.vertices;
      results = [];
      for (i = k = 0, len = ref.length; k < len; i = ++k) {
        vertex = ref[i];
        if (vertex.edge) {
          edge = {
            vertex1: vertex,
            vertex2: i === this.block.vertices.length - 1 ? this.block.vertices[0] : this.block.vertices[i + 1],
            block: this.block,
            texture: vertex.edge,
            theme: this.assets.theme.edge_params(vertex.edge)
          };
          edge.angle = Math2D.angle_between_points(edge.vertex1, edge.vertex2) - Math.PI / 2;
          edge.vertices = [
            {
              x: edge.vertex1.absolute_x,
              y: edge.vertex1.absolute_y - edge.theme.depth
            },
            {
              x: edge.vertex2.absolute_x,
              y: edge.vertex2.absolute_y - edge.theme.depth
            },
            {
              x: edge.vertex2.absolute_x,
              y: edge.vertex2.absolute_y
            },
            {
              x: edge.vertex1.absolute_x,
              y: edge.vertex1.absolute_y
            }
          ];
          edge.aabb = this.compute_aabb(edge);
          results.push(this.list.push(edge));
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    load_assets() {
      var edge, k, len, ref, results;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        edge = ref[k];
        results.push(this.assets.effects.push(edge.theme.file));
      }
      return results;
    }

    init() {
      return this.init_sprites();
    }

    init_sprites() {
      var edge, k, len, matrix, points, ref, results, texture;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        edge = ref[k];
        // Build polygon in PIXI coordinates (y inverted)
        points = edge.vertices.map(function(vertex) {
          return new PIXI.Point(vertex.x, -vertex.y);
        });
        // Load edge texture
        texture = PIXI.Texture.from(this.assets.get_url(edge.theme.file));
        // Matrix maps world coords → texture UV, pivoting around top-left corner.
        matrix = new PIXI.Matrix();
        matrix.scale(1.0 / 110.0, 1.0 / 110.0 * edge.theme.scale * edge.theme.depth); // reverse engineering to get nice visual values
        matrix.rotate(-edge.angle);
        matrix.translate(edge.vertex1.absolute_x, -edge.vertex1.absolute_y);
        // Create graphics with texture and matrix
        edge.graphics = new PIXI.Graphics();
        edge.graphics.poly(points);
        edge.graphics.fill({
          texture: texture,
          matrix: matrix,
          color: 0xffffff,
          alpha: 1.0,
          textureSpace: 'global'
        });
        results.push(this.level.layers.static_level.addChild(edge.graphics));
      }
      return results;
    }

    // only display edges present on the screen zone
    update() {
      var block_visible, edge, k, len, ref, results;
      if (!Constants.debug_physics) {
        block_visible = this.block.graphics.visible;
        ref = this.list;
        results = [];
        for (k = 0, len = ref.length; k < len; k++) {
          edge = ref[k];
          results.push(edge.graphics.visible = block_visible && this.visible(edge)); // don't test aabb if block not visible
        }
        return results;
      }
    }

    compute_aabb(edge) {
      var aabb, x_positions, y_positions;
      x_positions = edge.vertices.map(function(v) {
        return v.x;
      });
      y_positions = edge.vertices.map(function(v) {
        return v.y;
      });
      aabb = new Box2D.Collision.b2AABB();
      aabb.lowerBound.Set(Math.min.apply(null, x_positions), Math.min.apply(null, y_positions));
      aabb.upperBound.Set(Math.max.apply(null, x_positions), Math.max.apply(null, y_positions));
      return aabb;
    }

    visible(edge) {
      return edge.aabb.TestOverlap(this.level.camera.aabb);
    }

  };

  // New Edges implementation:
  // -> 1 polygon for the continuous vertices with texture
  // -> MeshGeometry with uvs for texture position
  // -> 1 GPU call for full edge
  Edges = class Edges {
    constructor(level, block) {
      this.level = level;
      this.block = block;
      this.assets = this.level.assets;
      this.polygons = []; // Each polygon is an edge that was created using the contiguous vertices sharing the same edge texture
    }

    parse() {
      var k, len, polygon, results, run, runs, theme;
      runs = this.extract_runs(this.block.vertices);
      results = [];
      for (k = 0, len = runs.length; k < len; k++) {
        run = runs[k];
        theme = this.assets.theme.edge_params(run.texture);
        polygon = this.build_edge_polygon(this.block.vertices, run, theme);
        polygon.aabb = this.compute_aabb(polygon);
        results.push(this.polygons.push(polygon));
      }
      return results;
    }

    // Find maximal runs of consecutive vertices sharing the same edge texture.
    extract_runs(vertices) {
      var first, i, j, last, n, runs, texture;
      n = vertices.length;
      runs = [];
      i = 0;
      while (i < n) {
        if (vertices[i].edge) {
          texture = vertices[i].edge;
          j = i + 1;
          while (j < n && vertices[j].edge === texture) {
            j += 1;
          }
          runs.push({
            start: i,
            end: j - 1,
            texture: texture
          });
          i = j;
        } else {
          i += 1;
        }
      }
      // Wrapping runs (first and last with same texture and continuous positions) are merged.
      if (runs.length >= 2) {
        first = runs[0];
        last = runs[runs.length - 1];
        if (first.start === 0 && last.end === n - 1 && first.texture === last.texture) {
          first.start = last.start;
          runs.pop();
        }
      }
      return runs;
    }

    // Build the geometry for a single edge polygon run.
    // Collects the surface vertices from run.start through (run.end + 1),
    // then builds a triangle strip projecting downward by theme.depth.
    build_edge_polygon(vertices, run, theme) {
      var bot_curr, bot_next, closing_idx, depth, dx, dy, i, indices, k, l, lengths, m, n, o, poly_vertices, ref, ref1, ref2, ref3, s, scale, surface, surface_count, uvs, v;
      n = vertices.length;
      depth = theme.depth;
      scale = theme.scale;
      // Collect surface vertices along this edge segment (may wrap)
      surface = [];
      closing_idx = (run.end + 1) % n;
      i = run.start;
      while (true) {
        v = vertices[i];
        surface.push({
          x: v.absolute_x,
          y: v.absolute_y
        });
        if (i === closing_idx) {
          break;
        }
        i = (i + 1) % n;
      }
      surface_count = surface.length;
      // Cumulative arc length along the surface → U texture coordinate
      lengths = [0];
      for (s = k = 1, ref = surface_count; (1 <= ref ? k < ref : k > ref); s = 1 <= ref ? ++k : --k) {
        dx = surface[s].x - surface[s - 1].x;
        dy = surface[s].y - surface[s - 1].y;
        lengths.push(lengths[s - 1] + Math.sqrt(dx * dx + dy * dy));
      }
      // Build vertex pairs: top row (surface vertices in order),
      // then bottom row (same vertices reversed, offset by -depth)
      poly_vertices = [];
      uvs = [];
      for (s = l = 0, ref1 = surface_count; (0 <= ref1 ? l < ref1 : l > ref1); s = 0 <= ref1 ? ++l : --l) {
        v = surface[s];
        poly_vertices.push({
          x: v.x,
          y: v.y
        });
        uvs.push(lengths[s] * scale, 0.0);
      }
      for (s = m = 0, ref2 = surface_count; (0 <= ref2 ? m < ref2 : m > ref2); s = 0 <= ref2 ? ++m : --m) {
        v = surface[surface_count - 1 - s];
        poly_vertices.push({
          x: v.x,
          y: v.y - depth
        });
        uvs.push(lengths[surface_count - 1 - s] * scale, 0.99);
      }
      // Triangle strip indices (2 triangles per segment)
      indices = [];
      for (s = o = 0, ref3 = surface_count - 1; (0 <= ref3 ? o < ref3 : o > ref3); s = 0 <= ref3 ? ++o : --o) {
        bot_curr = 2 * surface_count - 1 - s;
        bot_next = 2 * surface_count - 2 - s;
        indices.push(s, s + 1, bot_curr);
        indices.push(s + 1, bot_next, bot_curr);
      }
      return {
        theme: theme,
        vertices: poly_vertices,
        uvs: new Float32Array(uvs),
        indices: new Uint32Array(indices)
      };
    }

    load_assets() {
      var k, len, polygon, ref, results;
      ref = this.polygons;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        polygon = ref[k];
        results.push(this.assets.effects.push(polygon.theme.file));
      }
      return results;
    }

    init() {
      return this.init_sprites();
    }

    init_sprites() {
      var count, geometry, i, k, l, len, len1, polygon, positions, ref, ref1, results, texture, v;
      ref = this.polygons;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        polygon = ref[k];
        // Convert world coords (Y-up) to PIXI coords (Y-down)
        count = polygon.vertices.length;
        positions = new Float32Array(count * 2);
        ref1 = polygon.vertices;
        for (i = l = 0, len1 = ref1.length; l < len1; i = ++l) {
          v = ref1[i];
          positions[i * 2] = v.x;
          positions[i * 2 + 1] = -v.y;
        }
        geometry = new PIXI.MeshGeometry({
          positions: positions,
          uvs: polygon.uvs,
          indices: polygon.indices
        });
        texture = PIXI.Texture.from(this.assets.get_url(polygon.theme.file));
        texture.source.addressMode = 'repeat';
        polygon.mesh = new PIXI.Mesh({
          geometry: geometry,
          texture: texture
        });
        polygon.mesh.label = `Edge (${this.block.graphics.label})`;
        results.push(this.level.layers.layer_for_block(this.block).addChild(polygon.mesh));
      }
      return results;
    }

    // only display edges present on the screen zone
    update() {
      var block_visible, k, len, polygon, ref, results;
      if (!Constants.debug_physics) {
        block_visible = this.block.graphics.visible;
        ref = this.polygons;
        results = [];
        for (k = 0, len = ref.length; k < len; k++) {
          polygon = ref[k];
          results.push(polygon.mesh.visible = block_visible && this.visible(polygon));
        }
        return results;
      }
    }

    visible(polygon) {
      var parallax_layer;
      if (this.block.position.islayer && this.block.position.layerid !== void 0) {
        parallax_layer = this.level.layers.list[this.block.position.layerid];
        return polygon.aabb.TestOverlap(parallax_layer.camera_aabb);
      } else {
        return polygon.aabb.TestOverlap(this.level.camera.aabb);
      }
    }

    compute_aabb(polygon) {
      var aabb, x_positions, y_positions;
      x_positions = polygon.vertices.map(function(v) {
        return v.x;
      });
      y_positions = polygon.vertices.map(function(v) {
        return v.y;
      });
      aabb = new Box2D.Collision.b2AABB();
      aabb.lowerBound.Set(Math.min.apply(null, x_positions), Math.min.apply(null, y_positions));
      aabb.upperBound.Set(Math.max.apply(null, x_positions), Math.max.apply(null, y_positions));
      return aabb;
    }

  };

  Entities = class Entities {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
      this.world = level.physics.world;
      this.list = [];
      this.player_start = {
        x: 0,
        y: 0
      };
      this.strawberries = [];
      this.wreckers = [];
    }

    parse(xml) {
      var entity, k, l, len, len1, name, texture_name, theme_center_x, theme_center_y, theme_height, theme_sprite, theme_width, value, xml_entities, xml_entity, xml_param, xml_params;
      xml_entities = $(xml).find('entity');
// parse entity xml
      for (k = 0, len = xml_entities.length; k < len; k++) {
        xml_entity = xml_entities[k];
        entity = {
          id: $(xml_entity).attr('id'),
          typeid: $(xml_entity).attr('typeid'),
          size: {
            r: parseFloat($(xml_entity).find('size').attr('r')),
            z: parseInt($(xml_entity).find('size').attr('z')) || void 0,
            width: (function() {
              var attr;
              attr = $(xml_entity).find('size').attr('width');
              if (attr != null) {
                return parseFloat(attr);
              } else {
                return void 0; // integer or undefined if no attribute (will come from theme)
              }
            })(),
            height: (function() {
              var attr;
              attr = $(xml_entity).find('size').attr('height');
              if (attr != null) {
                return parseFloat(attr);
              } else {
                return void 0; // integer or undefined if no attribute (will come from theme)
              }
            })()
          },
          position: {
            x: parseFloat($(xml_entity).find('position').attr('x')),
            y: parseFloat($(xml_entity).find('position').attr('y')),
            angle: parseFloat($(xml_entity).find('position').attr('angle')) || 0,
            reversed: $(xml_entity).find('position').attr('reversed') === 'true'
          },
          center: {
            x: void 0, // can only come from theme
            y: void 0 // can only come from theme
          },
          params: {}
        };
        // parse params tags
        xml_params = $(xml_entity).find('param');
        for (l = 0, len1 = xml_params.length; l < len1; l++) {
          xml_param = xml_params[l];
          name = $(xml_param).attr('name');
          value = $(xml_param).attr('value');
          entity.params[name] = value;
        }
        // find correct z (z can be in <size z="?"> or in <param name="z" value="?">)
        entity['z'] = entity.size.z || parseInt(entity.params.z) || 0;
        // Get default values for sprite from theme
        texture_name = this.entity_texture_name(entity);
        if (texture_name) {
          theme_sprite = this.assets.theme.sprite_params(texture_name);
          if (theme_sprite) {
            entity.file = theme_sprite.file;
            entity.file_base = theme_sprite.file_base;
            entity.file_extension = theme_sprite.file_extension;
            entity.delay = theme_sprite.delay;
            entity.frames = theme_sprite.frames;
            entity.display = true; // if an entity has a texture, it needs to be displayed
            
            // Default theme values if not defined in XML
            theme_width = theme_sprite.size.width || 1.0;
            theme_height = theme_sprite.size.height || 1.0;
            theme_center_x = theme_sprite.center.x || 0.5;
            theme_center_y = theme_sprite.center.y || 0.5;
            // - When the level overrides width/height, we keep the sprite centered on the
            //   sprite's original centerX/centerY by shifting the center by half the size delta.
            // - If level doesn't overrides width/height, we simply use theme values
            if (entity.size.width) {
              entity.center.x = theme_center_x + (entity.size.width - theme_width) / 2;
            } else {
              entity.size.width = theme_width;
              entity.center.x = theme_center_x;
            }
            if (entity.size.height) {
              entity.center.y = theme_center_y + (entity.size.height - theme_height) / 2;
            } else {
              entity.size.height = theme_height;
              entity.center.y = theme_center_y;
            }
            entity.aabb = this.compute_aabb(entity);
          } else {
            console.error(`XMoto warning: texture file \"${texture_name}\" was not found in the theme and is ignored.`);
          }
        }
        this.list.push(entity);
      }
      // order by z-index ASC
      this.list.sort(function(a, b) {
        if (a.z > b.z) {
          return 1;
        }
        if (a.z < b.z) {
          return -1;
        }
        return 0;
      });
      return this;
    }

    load_assets() {
      var entity, i, k, len, ref, results;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        entity = ref[k];
        if (entity.display) {
          if (entity.frames === 0) {
            results.push(this.assets.anims.push(entity.file));
          } else {
            results.push((function() {
              var l, ref1, results1;
              results1 = [];
              for (i = l = 0, ref1 = entity.frames - 1; (0 <= ref1 ? l <= ref1 : l >= ref1); i = 0 <= ref1 ? ++l : --l) {
                results1.push(this.assets.anims.push(this.frame_name(entity, i)));
              }
              return results1;
            }).call(this));
          }
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    init() {
      this.init_physics();
      this.init_sprites();
      return this.init_culling_debug();
    }

    init_physics() {
      var entity, k, len, ref, results;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        entity = ref[k];
        // End of level
        if (entity.typeid === 'EndOfLevel') {
          this.create_entity_physics(entity, 'end_of_level');
          results.push(this.end_of_level = entity);
        // Strawberries
        } else if (entity.typeid === 'Strawberry') {
          this.create_entity_physics(entity, 'strawberry');
          results.push(this.strawberries.push(entity));
        // Wreckers
        } else if (entity.typeid === 'Wrecker') {
          this.create_entity_physics(entity, 'wrecker');
          results.push(this.wreckers.push(entity));
        // Player start
        } else if (entity.typeid === 'PlayerStart') {
          results.push(this.player_start = {
            x: entity.position.x,
            y: entity.position.y
          });
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    create_entity_physics(entity, name) {
      var body, bodyDef, fixDef;
      // Create fixture
      fixDef = new Box2D.Dynamics.b2FixtureDef();
      fixDef.shape = new b2CircleShape(entity.size.r);
      fixDef.isSensor = true;
      // Create body
      bodyDef = new b2BodyDef();
      // Assign body position
      bodyDef.position.x = entity.position.x;
      bodyDef.position.y = entity.position.y;
      bodyDef.userData = {
        name: name,
        entity: entity
      };
      bodyDef.type = b2Body.b2_staticBody;
      // Assign fixture to body and add body to 2D world
      body = this.world.CreateBody(bodyDef);
      body.CreateFixture(fixDef);
      return body;
    }

    init_sprites() {
      var entity, k, len, ref, results;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        entity = ref[k];
        if (entity.z === 0) {
          results.push(this.init_sprite(entity, this.level.layers.static_level));
        } else if (entity.z < 0) {
          results.push(this.init_sprite(entity, this.level.layers.static_background));
        } else if (entity.z > 0) {
          results.push(this.init_sprite(entity, this.level.layers.static_foreground));
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    init_sprite(entity, container) {
      var i, k, ref, sprite, textures;
      if (entity.frames > 0) {
        textures = [];
        for (i = k = 0, ref = entity.frames - 1; (0 <= ref ? k <= ref : k >= ref); i = 0 <= ref ? ++k : --k) {
          textures.push(PIXI.Texture.from(this.assets.get_url(this.frame_name(entity, i))));
        }
        sprite = new PIXI.AnimatedSprite(textures);
        sprite.animationSpeed = (1.0 / entity.delay) / Constants.fps;
        sprite.play();
      } else if (entity.file) {
        sprite = PIXI.Sprite.from(this.assets.get_url(entity.file));
      }
      if (sprite) {
        sprite.width = entity.size.width;
        sprite.height = entity.size.height;
        sprite.anchor.x = entity.center.x / entity.size.width;
        sprite.anchor.y = 1 - (entity.center.y / entity.size.height);
        sprite.x = entity.position.x;
        sprite.y = -entity.position.y;
        sprite.rotation = -entity.position.angle;
        sprite.name = entity.params.name || entity.typeid;
        if (entity.position.reversed) {
          sprite.scale.x *= -1;
        }
        entity.sprite = sprite; // keep reference to the sprite
        return container.addChild(sprite);
      }
    }

    init_culling_debug() {
      if (Constants.debug_culling) {
        this.culling_debug = new PIXI.Graphics();
        this.culling_debug.label = 'culling (entities)';
        return this.level.layers.translate_layer.addChild(this.culling_debug);
      }
    }

    update(entity) {
      var k, len, ref;
      if (!Constants.debug_physics) {
        ref = this.list;
        for (k = 0, len = ref.length; k < len; k++) {
          entity = ref[k];
          if (entity.sprite) {
            entity.sprite.visible = this.visible(entity);
          }
        }
      }
      if (Constants.debug_culling) {
        return this.draw_debug_culling();
      }
    }

    draw_debug_culling() {
      var entity, k, len, line_width, ref;
      this.culling_debug.clear();
      ref = this.list;
      for (k = 0, len = ref.length; k < len; k++) {
        entity = ref[k];
        if (entity.aabb && entity.sprite.visible) {
          this.culling_debug.rect(entity.aabb.lowerBound.x, -entity.aabb.upperBound.y, entity.aabb.upperBound.x - entity.aabb.lowerBound.x, entity.aabb.upperBound.y - entity.aabb.lowerBound.y);
        }
      }
      line_width = 0.04 * Constants.default_scale.x / this.level.camera.scale.x;
      return this.culling_debug.stroke({
        width: line_width,
        color: 0x78C7C7,
        alpha: 0.7
      });
    }

    entity_texture_name(entity) {
      var name, ref;
      if (entity.typeid === 'Sprite') {
        name = entity.params.name;
      } else if (entity.typeid === 'EndOfLevel') { // hard-coded entity
        name = 'Flower';
      } else if ((ref = entity.typeid) === 'Strawberry' || ref === 'Wrecker') { // hard-coded entities
        name = entity.typeid;
      }
      // theme_replacements in XML
      if (this.level.replacements.sprites[name]) {
        name = this.level.replacements.sprites[name];
      }
      return name;
    }

    compute_aabb(entity) {
      var aabb, bottom, corner, corners, k, left, len, right, rotated, top, xs, ys;
      // limits relative to anchor
      left = -entity.center.x;
      right = left + entity.size.width;
      bottom = -entity.center.y;
      top = bottom + entity.size.height;
      corners = [
        {
          x: left,
          y: bottom
        },
        {
          x: right,
          y: bottom
        },
        {
          x: right,
          y: top
        },
        {
          x: left,
          y: top
        }
      ];
      // We always rotate the 4 corners by the entity.position.angle (usually 0!)
      // with entity.position as anchor
      xs = [];
      ys = [];
      for (k = 0, len = corners.length; k < len; k++) {
        corner = corners[k];
        rotated = Math2D.rotate_point(corner, entity.position.angle, entity.position);
        xs.push(rotated.x);
        ys.push(rotated.y);
      }
      aabb = new Box2D.Collision.b2AABB();
      aabb.lowerBound.Set(Math.min(...xs), Math.min(...ys));
      aabb.upperBound.Set(Math.max(...xs), Math.max(...ys));
      return aabb;
    }

    visible(entity) {
      return entity.display && entity.aabb.TestOverlap(this.level.camera.aabb);
    }

    frame_name(entity, frame_number) {
      return `${entity.file_base}${(frame_number / 100.0).toFixed(2).toString().substring(2)}.${entity.file_extension}`;
    }

  };

  Infos = class Infos {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
    }

    parse(xml) {
      var xml_border, xml_infos, xml_level, xml_music;
      xml_level = $(xml).find('level');
      this.identifier = xml_level.attr('id');
      this.pack_name = xml_level.attr('levelpack');
      this.pack_id = xml_level.attr('levelpackNum');
      this.r_version = xml_level.attr('rversion');
      xml_infos = $(xml).find('level').find('info');
      this.name = xml_infos.find('name').text();
      this.description = xml_infos.find('description').text();
      this.author = xml_infos.find('author').text();
      this.date = xml_infos.find('date').text();
      xml_border = xml_infos.find('border');
      this.border = xml_border.attr('texture');
      xml_music = xml_infos.find('music');
      this.music = xml_music.attr('name');
      return this;
    }

  };

  Layers = class Layers {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
      this.list = [];
      // Camera layers
      this.scale_layer = void 0;
      this.translate_layer = void 0;
      // Static layers
      this.static_background = void 0;
      this.static_level = void 0;
      this.static_foreground = void 0;
      // Parallax layers — each has its own scale/translate containers
      this.parallax_scale_layers = []; // direct children of stage for scaling
      this.parallax_translate_layers = []; // translate containers inside their container with parallax values
      this.parallax_layers = []; // parallax blocks
    }

    parse(xml) {
      var i, k, layer, len, xml_layer, xml_layers;
      xml_layers = $(xml).find('layeroffsets layeroffset');
      for (i = k = 0, len = xml_layers.length; k < len; i = ++k) {
        xml_layer = xml_layers[i];
        layer = {
          x: parseFloat($(xml_layer).attr('x')),
          y: parseFloat($(xml_layer).attr('y')),
          frontlayer: $(xml_layer).attr('frontlayer') === 'true'
        };
        layer.name = $(xml_layer).attr('name') || `parallax-${layer.frontlayer ? 'front' : 'back'}-${i}`;
        this.list.push(layer);
      }
      return this;
    }

    init() {
      this.init_camera_layers();
      this.init_static_layers();
      this.init_parallax_layers();
      return this.assemble_layers();
    }

    // Layers for easier camera control
    init_camera_layers() {
      this.scale_layer = new PIXI.Container();
      this.translate_layer = new PIXI.Container();
      this.scale_layer.label = 'level - scale';
      return this.translate_layer.label = 'level - translate';
    }

    // Static level layers
    init_static_layers() {
      this.static_background = new PIXI.Container(); // entity.z < 0 or blocks with background="true"
      this.static_level = new PIXI.Container(); // entity.z = 0 or blocks with classic blocks
      this.static_foreground = new PIXI.Container(); // entity.z > 0 or blocks with islayer="true" and no layerid
      this.static_background.label = 'background';
      this.static_level.label = 'level';
      return this.static_foreground.label = 'foreground';
    }

    init_parallax_layers() {
      var k, len, parallax_data, parallax_layer, ref, results, scale_layer, translate_layer;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        parallax_data = ref[k];
        scale_layer = new PIXI.Container();
        translate_layer = new PIXI.Container();
        parallax_layer = new PIXI.Container();
        scale_layer.label = `${parallax_data.name} - scale`;
        translate_layer.label = `${parallax_data.name} - translate`;
        parallax_layer.label = `${parallax_data.name}`;
        parallax_layer.parallax = true;
        scale_layer.addChild(translate_layer);
        translate_layer.addChild(parallax_layer);
        this.parallax_scale_layers.push(scale_layer);
        this.parallax_translate_layers.push(translate_layer);
        results.push(this.parallax_layers.push(parallax_layer));
      }
      return results;
    }

    assemble_layers() {
      var i, k, l, len, len1, parallax, ref, ref1, results;
      ref = this.list;
      /* Structure is like this
         Root
          - Sky
          - n Scale > Translate > Parallax layers with frontlayer="false"
          - Scale
            -> Translate
              -> static-background
              -> static-level
              -> static-foreground
          - n Scale > Translate > Parallax layers with frontlayer="true"
         */
      for (i = k = 0, len = ref.length; k < len; i = ++k) {
        parallax = ref[i];
        if (!parallax.frontlayer) {
          this.level.stage.addChild(this.parallax_scale_layers[i]);
        }
      }
      this.level.stage.addChild(this.scale_layer);
      this.scale_layer.addChild(this.translate_layer);
      this.translate_layer.addChild(this.static_background);
      this.translate_layer.addChild(this.static_level);
      this.translate_layer.addChild(this.static_foreground);
      ref1 = this.list;
      results = [];
      for (i = l = 0, len1 = ref1.length; l < len1; i = ++l) {
        parallax = ref1[i];
        if (parallax.frontlayer) {
          results.push(this.level.stage.addChild(this.parallax_scale_layers[i]));
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    layer_for_block(block) {
      if (block.position.background) {
        return this.static_background;
      } else if (block.position.islayer) {
        if (block.position.layerid !== void 0) {
          return this.parallax_layers[block.position.layerid];
        } else {
          return this.static_foreground;
        }
      } else {
        return this.static_level;
      }
    }

    update() {}

  };

  Limits = class Limits {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
    }

    parse(xml) {
      var k, len, ref, wall, xml_limits;
      xml_limits = $(xml).find('limits');
      // CAREFUL ! The limits on files are not real, some polygons could be outside
      // => It seems to be the limits where the player can go
      this.player = {
        left: parseFloat(xml_limits.attr('left')),
        right: parseFloat(xml_limits.attr('right')),
        top: parseFloat(xml_limits.attr('top')),
        bottom: parseFloat(xml_limits.attr('bottom'))
      };
      this.screen = {
        left: this.player.left - 20,
        right: this.player.right + 20,
        top: this.player.top + 20,
        bottom: this.player.bottom - 20
      };
      this.size = {
        x: this.screen.right - this.screen.left,
        y: this.screen.top - this.screen.bottom
      };
      // Each wall is a rectangle (in Y-up world coords) — its AABB, physics polygon,
      // and TilingSprite are all derived from these four bounds.
      this.walls = [
        {
          name: 'left',
          left: this.screen.left,
          right: this.player.left,
          bottom: this.screen.bottom,
          top: this.screen.top
        },
        {
          name: 'right',
          left: this.player.right,
          right: this.screen.right,
          bottom: this.screen.bottom,
          top: this.screen.top
        },
        {
          name: 'bottom',
          left: this.player.left,
          right: this.player.right,
          bottom: this.screen.bottom,
          top: this.player.bottom
        },
        {
          name: 'top',
          left: this.player.left,
          right: this.player.right,
          bottom: this.player.top,
          top: this.screen.top
        }
      ];
      ref = this.walls;
      // Compute AABB
      for (k = 0, len = ref.length; k < len; k++) {
        wall = ref[k];
        wall.aabb = this.compute_aabb(wall);
      }
      this.texture = this.level.infos.border || 'dirt';
      this.texture_name = this.assets.theme.texture_params(this.texture).file;
      return this;
    }

    load_assets() {
      return this.assets.textures.push(this.texture_name);
    }

    init() {
      this.init_physics();
      this.init_sprites();
      return this.init_culling_debug();
    }

    init_physics() {
      var ground, k, len, ref, results, vertices, wall;
      ground = Constants.ground;
      ref = this.walls;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        wall = ref[k];
        vertices = [
          {
            x: wall.left,
            y: wall.top
          },
          {
            x: wall.left,
            y: wall.bottom
          },
          {
            x: wall.right,
            y: wall.bottom
          },
          {
            x: wall.right,
            y: wall.top
          }
        ];
        results.push(this.level.physics.create_polygon(vertices, 'ground', ground.density, ground.restitution, ground.friction));
      }
      return results;
    }

    init_sprites() {
      var k, len, ref, results, texture, wall;
      texture = PIXI.Texture.from(this.assets.get_url(this.texture_name));
      ref = this.walls;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        wall = ref[k];
        wall.sprite = new PIXI.TilingSprite({
          texture: texture,
          width: wall.right - wall.left,
          height: wall.top - wall.bottom
        });
        wall.sprite.x = wall.left;
        wall.sprite.y = -wall.top;
        wall.sprite.anchor.x = 0;
        wall.sprite.anchor.y = 0;
        wall.sprite.tileScale.x = 1.0 / 60.0;
        wall.sprite.tileScale.y = 1.0 / 60.0;
        results.push(this.level.layers.static_level.addChild(wall.sprite));
      }
      return results;
    }

    init_culling_debug() {
      if (Constants.debug_culling) {
        this.culling_debug = new PIXI.Graphics();
        this.culling_debug.label = 'culling (limits)';
        return this.level.layers.translate_layer.addChild(this.culling_debug);
      }
    }

    update() {
      var k, len, ref, wall;
      if (!Constants.debug_physics) {
        ref = this.walls;
        for (k = 0, len = ref.length; k < len; k++) {
          wall = ref[k];
          wall.sprite.visible = this.visible(wall);
        }
      }
      if (Constants.debug_culling) {
        return this.draw_debug_culling();
      }
    }

    draw_debug_culling() {
      var k, len, line_width, ref, wall;
      this.culling_debug.clear();
      ref = this.walls;
      for (k = 0, len = ref.length; k < len; k++) {
        wall = ref[k];
        if (wall.sprite.visible) {
          this.culling_debug.rect(wall.aabb.lowerBound.x, -wall.aabb.upperBound.y, wall.aabb.upperBound.x - wall.aabb.lowerBound.x, wall.aabb.upperBound.y - wall.aabb.lowerBound.y);
        }
      }
      line_width = 0.04 * Constants.default_scale.x / this.level.camera.scale.x;
      return this.culling_debug.stroke({
        width: line_width,
        color: 0xC778C7,
        alpha: 0.7
      });
    }

    visible(wall) {
      return wall.aabb.TestOverlap(this.level.camera.aabb);
    }

    compute_aabb(wall) {
      var aabb;
      aabb = new Box2D.Collision.b2AABB();
      aabb.lowerBound.Set(wall.left, wall.bottom);
      aabb.upperBound.Set(wall.right, wall.top);
      return aabb;
    }

  };

  Replacements = class Replacements {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
      this.sprites = {}; // old_name => new_name
      this.sounds = {}; // old_name => new_name
    }

    parse(xml) {
      var k, l, len, len1, new_name, old_name, ref, ref1, results, sound_replacement, sprite_replacement, xml_replacements;
      xml_replacements = $(xml).find('theme_replacements');
      ref = xml_replacements.find('sprite_replacement');
      for (k = 0, len = ref.length; k < len; k++) {
        sprite_replacement = ref[k];
        old_name = $(sprite_replacement).attr('old_name');
        new_name = $(sprite_replacement).attr('new_name');
        this.sprites[old_name] = new_name;
      }
      ref1 = xml_replacements.find('sound_replacement');
      results = [];
      for (l = 0, len1 = ref1.length; l < len1; l++) {
        sound_replacement = ref1[l];
        old_name = $(sound_replacement).attr('old_name');
        new_name = $(sound_replacement).attr('new_name');
        results.push(this.sounds[old_name] = new_name);
      }
      return results;
    }

  };

  Script = class Script {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
    }

    parse(xml) {
      var xml_script;
      xml_script = $(xml).find('script');
      this.code = xml_script.text();
      return this;
    }

    init() {}

    display(ctx) {}

  };

  Sky = class Sky {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
      this.options = level.options;
    }

    parse(xml) {
      var xml_sky;
      xml_sky = $(xml).find('level info sky');
      this.name = xml_sky.text().toLowerCase();
      this.color_r = parseInt(xml_sky.attr('color_r'));
      this.color_g = parseInt(xml_sky.attr('color_g'));
      this.color_b = parseInt(xml_sky.attr('color_b'));
      this.color_a = parseInt(xml_sky.attr('color_a'));
      this.zoom = parseFloat(xml_sky.attr('zoom'));
      this.offset = parseFloat(xml_sky.attr('offset'));
      if (this.name === '') {
        this.name = 'sky1';
      }
      this.filename = this.assets.theme.texture_params(this.name).file;
      return this;
    }

    load_assets() {
      return this.assets.textures.push(this.filename);
    }

    init() {
      return this.init_sprites();
    }

    init_sprites() {
      var texture;
      texture = PIXI.Texture.from(this.assets.get_url(this.filename));
      this.sprite = new PIXI.TilingSprite({
        texture: texture,
        width: this.options.width,
        height: this.options.height
      });
      this.sprite.label = "sky";
      this.sprite.position.x = 0;
      this.sprite.position.y = 0;
      this.sprite.tileScale.x = 4;
      this.sprite.tileScale.y = 4;
      return this.level.stage.addChildAt(this.sprite, 0); // Fixed on the root level of stage (not influenced by scale/translation, only adapt tilePosition)
    }

    update() {
      var ctx, parallax_x, parallax_y;
      ctx = this.level.debug_ctx;
      if (Constants.debug_physics) {
        ctx.beginPath();
        ctx.moveTo(this.options.width, this.options.height);
        ctx.lineTo(0, this.options.height);
        ctx.lineTo(0, 0);
        ctx.lineTo(this.options.width, 0);
        ctx.closePath();
        ctx.fillStyle = "#222228";
        return ctx.fill();
      } else {
        if (this.sprite.width !== this.options.width) {
          // Only when going in/out fullscreen (avoid some internal computations)
          this.sprite.width = this.options.width;
        }
        if (this.sprite.height !== this.options.height) {
          this.sprite.height = this.options.height;
        }
        parallax_x = 15; // sky parallax on x axix
        parallax_y = 7; // sky parallax on y axis
        this.sprite.tilePosition.x = -this.level.camera.target().x * parallax_x;
        return this.sprite.tilePosition.y = this.level.camera.target().y * parallax_y;
      }
    }

  };

  Ghost = class Ghost {
    constructor(level, replay, transparent = true) {
      this.level = level;
      this.replay = replay;
      this.transparent = transparent;
      this.moto = new Moto(this.level, this.transparent);
    }

    init() {
      return this.moto.init();
    }

    reload() {
      this.moto.destroy();
      this.moto = new Moto(this.level, this.transparent);
      return this.moto.init();
    }

    move() {
      this.move_with_input(); // every steps
      return this.move_with_key_step(); // only when key step
    }

    move_with_input() {
      var current_input;
      current_input = {
        up: this.replay.is_down('up'),
        down: this.replay.is_down('down'),
        left: this.replay.is_down('left'),
        right: this.replay.is_down('right'),
        space: this.replay.is_pressed('space')
      };
      return this.moto.move(current_input);
    }

    move_with_key_step() {
      var k, key_step, l, len, len1, part, ref, ref1, results;
      key_step = this.replay.key_steps[this.level.physics.steps];
      if (key_step) {
        ref = ['body', 'left_wheel', 'right_wheel', 'left_axle', 'right_axle'];
        for (k = 0, len = ref.length; k < len; k++) {
          part = ref[k];
          this.set_part_position(this.moto, part, key_step);
        }
        ref1 = ['torso', 'upper_leg', 'lower_leg', 'upper_arm', 'lower_arm'];
        results = [];
        for (l = 0, len1 = ref1.length; l < len1; l++) {
          part = ref1[l];
          results.push(this.set_part_position(this.moto.rider, part, key_step));
        }
        return results;
      }
    }

    update() {
      return this.moto.update();
    }

    set_part_position(entity, part, key_step) {
      entity[part].SetPosition({
        x: key_step[part].position.x,
        y: key_step[part].position.y
      });
      entity[part].SetAngle(key_step[part].angle);
      entity[part].SetLinearVelocity({
        x: key_step[part].linear_velocity.x,
        y: key_step[part].linear_velocity.y
      });
      return entity[part].GetAngularVelocity(key_step[part].angular_velocity);
    }

  };

  Ghosts = class Ghosts {
    constructor(level) {
      this.level = level;
      this.assets = level.assets;
      this.options = level.options;
      this.player = void 0;
      this.others = [];
      this.load_replays();
    }

    load_assets() {
      var k, len, part, parts, results;
      // Assets
      parts = [Constants.torso, Constants.upper_leg, Constants.lower_leg, Constants.upper_arm, Constants.lower_arm, Constants.body, Constants.left_wheel, Constants.right_wheel, Constants.left_axle, Constants.right_axle];
      results = [];
      for (k = 0, len = parts.length; k < len; k++) {
        part = parts[k];
        results.push(this.assets.moto.push(part.ghost_texture));
      }
      return results;
    }

    all_ghosts() {
      var ghosts;
      ghosts = [];
      ghosts = ghosts.concat(this.others);
      if (this.player) {
        ghosts.push(this.player);
      }
      return ghosts;
    }

    init() {
      var ghost, k, len, ref, results;
      ref = this.all_ghosts();
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        ghost = ref[k];
        if (ghost.replay) {
          results.push(ghost.init());
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    reload() {
      var ghost, k, len, ref, results;
      ref = this.all_ghosts();
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        ghost = ref[k];
        if (ghost.replay) {
          results.push(ghost.reload());
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    move() {
      var ghost, k, len, ref, results;
      ref = this.all_ghosts();
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        ghost = ref[k];
        if (ghost.replay) {
          results.push(ghost.move());
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    update() {
      var ghost, k, len, ref, results;
      ref = this.all_ghosts();
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        ghost = ref[k];
        if (ghost.replay) {
          results.push(ghost.update());
        } else {
          results.push(void 0);
        }
      }
      return results;
    }

    load_replays() {
      var k, len, option_replay, ref, replay, results;
      ref = this.options.replays;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        option_replay = ref[k];
        replay = new Replay(this.level);
        replay.load(option_replay.replay);
        if (!this.options.playable && option_replay.follow) {
          results.push(this.player = new Ghost(this.level, replay, false));
        } else {
          results.push(this.others.push(new Ghost(this.level, replay)));
        }
      }
      return results;
    }

  };

  b2Vec2 = Box2D.Common.Math.b2Vec2;

  b2BodyDef = Box2D.Dynamics.b2BodyDef;

  b2Body = Box2D.Dynamics.b2Body;

  b2FixtureDef = Box2D.Dynamics.b2FixtureDef;

  b2Fixture = Box2D.Dynamics.b2Fixture;

  b2PolygonShape = Box2D.Collision.Shapes.b2PolygonShape;

  b2CircleShape = Box2D.Collision.Shapes.b2CircleShape;

  b2PrismaticJointDef = Box2D.Dynamics.Joints.b2PrismaticJointDef;

  b2RevoluteJointDef = Box2D.Dynamics.Joints.b2RevoluteJointDef;

  Moto = class Moto {
    constructor(level, ghost = false) {
      this.level = level;
      this.assets = level.assets;
      this.world = level.physics.world;
      this.mirror = 1; // 1 = right-oriented, -1 = left-oriented
      this.dead = false;
      this.ghost = ghost;
      this.rider = new Rider(level, this);
    }

    destroy() {
      this.rider.destroy();
      // physics
      this.world.DestroyBody(this.body);
      this.world.DestroyBody(this.left_wheel);
      this.world.DestroyBody(this.right_wheel);
      this.world.DestroyBody(this.left_axle);
      this.world.DestroyBody(this.right_axle);
      // graphics
      this.level.layers.static_level.removeChild(this.body_sprite);
      this.level.layers.static_level.removeChild(this.left_wheel_sprite);
      this.level.layers.static_level.removeChild(this.right_wheel_sprite);
      this.level.layers.static_level.removeChild(this.left_axle_sprite);
      return this.level.layers.static_level.removeChild(this.right_axle_sprite);
    }

    load_assets() {
      var k, len, part, parts;
      parts = [Constants.body, Constants.left_wheel, Constants.right_wheel, Constants.left_axle, Constants.right_axle];
      for (k = 0, len = parts.length; k < len; k++) {
        part = parts[k];
        if (this.ghost) {
          this.assets.moto.push(part.ghost_texture);
        } else {
          this.assets.moto.push(part.texture);
        }
      }
      return this.rider.load_assets();
    }

    init() {
      this.init_physics();
      return this.init_sprites();
    }

    init_physics() {
      this.player_start = this.level.entities.player_start;
      this.body = this.create_body();
      this.left_wheel = this.create_wheel(Constants.left_wheel);
      this.right_wheel = this.create_wheel(Constants.right_wheel);
      this.left_axle = this.create_axle(Constants.left_axle);
      this.right_axle = this.create_axle(Constants.right_axle);
      this.left_revolute_joint = this.create_revolute_joint(this.left_axle, this.left_wheel);
      this.right_revolute_joint = this.create_revolute_joint(this.right_axle, this.right_wheel);
      this.left_prismatic_joint = this.create_prismatic_joint(this.left_axle, Constants.left_suspension);
      this.right_prismatic_joint = this.create_prismatic_joint(this.right_axle, Constants.right_suspension);
      return this.rider.init_physics();
    }

    init_sprites() {
      var asset_name, axle_name, k, l, len, len1, len2, m, part, ref, ref1, ref2, wheel_name;
      ref = ['body', 'left_wheel', 'right_wheel', 'left_axle', 'right_axle'];
      // Create and add sprites to the scene
      for (k = 0, len = ref.length; k < len; k++) {
        part = ref[k];
        if (this.ghost) {
          asset_name = Constants[part].ghost_texture;
        } else {
          asset_name = Constants[part].texture;
        }
        this[`${part}_sprite`] = PIXI.Sprite.from(this.assets.get_url(asset_name));
        this[`${part}_sprite`].label = part;
        this.level.layers.static_level.addChild(this[`${part}_sprite`]);
      }
      // Define body values
      this.body_sprite.width = Constants.body.texture_size.x;
      this.body_sprite.height = Constants.body.texture_size.y;
      this.body_sprite.anchor.x = 0.5;
      this.body_sprite.anchor.y = 0.5;
      ref1 = ['left_wheel', 'right_wheel'];
      // Define wheels values
      for (l = 0, len1 = ref1.length; l < len1; l++) {
        wheel_name = ref1[l];
        this[`${wheel_name}_sprite`].width = 2 * Constants[wheel_name].radius;
        this[`${wheel_name}_sprite`].height = 2 * Constants[wheel_name].radius;
        this[`${wheel_name}_sprite`].anchor.x = 0.5;
        this[`${wheel_name}_sprite`].anchor.y = 0.5;
      }
      ref2 = ['left_axle', 'right_axle'];
      // Define axles values
      for (m = 0, len2 = ref2.length; m < len2; m++) {
        axle_name = ref2[m];
        this[`${axle_name}_sprite`].anchor.x = 0.0;
        this[`${axle_name}_sprite`].anchor.y = 0.5;
      }
      return this.rider.init_sprites();
    }

    move(input = this.level.input) {
      var air_density, back_force, biker_force, drag_force, moto_acceleration, object_penetration, rigidity, squared_speed, v;
      moto_acceleration = Constants.moto_acceleration;
      biker_force = Constants.biker_force;
      if (!this.dead) {
        // Accelerate
        if (input.up) {
          this.left_wheel.ApplyTorque(-this.mirror * moto_acceleration);
        }
        // Brakes
        if (input.down) {
          // block wheels
          this.right_wheel.SetAngularVelocity(0);
          this.left_wheel.SetAngularVelocity(0);
        }
        // Back wheeling
        if ((input.left && this.mirror === 1) || (input.right && this.mirror === -1)) {
          this.wheeling(biker_force);
        }
        // Front wheeling
        if ((input.right && this.mirror === 1) || (input.left && this.mirror === -1)) {
          biker_force = -biker_force * 0.8; // a bit less force for front wheeling
          this.wheeling(biker_force);
        }
        if (input.space) {
          this.flip();
        }
      }
      if (!input.up && !input.down) {
        // Engine brake
        v = this.left_wheel.GetAngularVelocity();
        this.left_wheel.ApplyTorque((Math.abs(v) >= 0.2 ? -v / 10 : void 0));
        // Friction on right wheel
        v = this.right_wheel.GetAngularVelocity();
        this.right_wheel.ApplyTorque((Math.abs(v) >= 0.2 ? -v / 100 : void 0));
      }
      // Left wheel suspension
      back_force = Constants.left_suspension.back_force;
      rigidity = Constants.left_suspension.rigidity;
      this.left_prismatic_joint.SetMaxMotorForce(rigidity + Math.abs(rigidity * 100 * Math.pow(this.left_prismatic_joint.GetJointTranslation(), 2)));
      this.left_prismatic_joint.SetMotorSpeed(-back_force * this.left_prismatic_joint.GetJointTranslation());
      // Right wheel suspension
      back_force = Constants.right_suspension.back_force;
      rigidity = Constants.right_suspension.rigidity;
      this.right_prismatic_joint.SetMaxMotorForce(rigidity + Math.abs(rigidity * 100 * Math.pow(this.right_prismatic_joint.GetJointTranslation(), 2)));
      this.right_prismatic_joint.SetMotorSpeed(-back_force * this.right_prismatic_joint.GetJointTranslation());
      // Drag (air resistance)
      air_density = Constants.air_density;
      object_penetration = 0.025;
      squared_speed = Math.pow(this.body.GetLinearVelocity().x, 2);
      drag_force = air_density * squared_speed * object_penetration;
      this.body.SetLinearDamping(drag_force);
      // Limitation of wheel rotation speed (and by extension, of moto)
      if (this.right_wheel.GetAngularVelocity() > Constants.max_moto_speed) {
        this.right_wheel.SetAngularVelocity(Constants.max_moto_speed);
      } else if (this.right_wheel.GetAngularVelocity() < -Constants.max_moto_speed) {
        this.right_wheel.SetAngularVelocity(-Constants.max_moto_speed);
      }
      if (this.left_wheel.GetAngularVelocity() > Constants.max_moto_speed) {
        return this.left_wheel.SetAngularVelocity(Constants.max_moto_speed);
      } else if (this.left_wheel.GetAngularVelocity() < -Constants.max_moto_speed) {
        return this.left_wheel.SetAngularVelocity(-Constants.max_moto_speed);
      }
    }

    // Detection of drifting
    //rotation_speed = -(moto.left_wheel.GetAngularVelocity()*Math.PI/180)*2*Math.PI*Constants.left_wheel.radius
    //linear_speed   = moto.left_wheel.GetLinearVelocity().x/10
    //if linear_speed > 0 and rotation_speed > 1.5*linear_speed
    //  @level.particles.create()
    wheeling(force) {
      var force_leg, force_torso, moto_angle;
      moto_angle = this.mirror * this.body.GetAngle();
      this.body.ApplyTorque(this.mirror * force * 0.50);
      force_torso = Math2D.rotate_point({
        x: this.mirror * (-force),
        y: 0
      }, moto_angle, {
        x: 0,
        y: 0
      });
      force_torso.y = this.mirror * force_torso.y;
      this.rider.torso.ApplyForce(force_torso, this.rider.torso.GetWorldCenter());
      force_leg = Math2D.rotate_point({
        x: this.mirror * force,
        y: 0
      }, moto_angle, {
        x: 0,
        y: 0
      });
      force_leg.y = this.mirror * force_leg.y;
      return this.rider.lower_leg.ApplyForce(force_leg, this.rider.lower_leg.GetWorldCenter());
    }

    flip() {
      if (!this.dead) {
        return MotoFlipService.execute(this);
      }
    }

    create_body() {
      var body, bodyDef, fixDef;
      // Create fixture
      fixDef = new b2FixtureDef();
      fixDef.shape = new b2PolygonShape();
      fixDef.density = Constants.body.density;
      fixDef.restitution = Constants.body.restitution;
      fixDef.friction = Constants.body.friction;
      fixDef.isSensor = !Constants.body.collisions;
      fixDef.filter.groupIndex = -1;
      Physics.create_shape(fixDef, Constants.body.shape, this.mirror === -1);
      // Create body
      bodyDef = new b2BodyDef();
      // Assign body position
      bodyDef.position.x = this.player_start.x + this.mirror * Constants.body.position.x;
      bodyDef.position.y = this.player_start.y + Constants.body.position.y;
      bodyDef.userData = {
        name: 'moto',
        type: this.ghost ? 'ghost' : 'player',
        moto: this
      };
      bodyDef.type = b2Body.b2_dynamicBody;
      // Assign fixture to body and add body to 2D world
      body = this.world.CreateBody(bodyDef);
      body.CreateFixture(fixDef);
      return body;
    }

    create_wheel(part_constants) {
      var bodyDef, fixDef, wheel;
      // Create fixture
      fixDef = new b2FixtureDef();
      fixDef.shape = new b2CircleShape(part_constants.radius);
      fixDef.density = part_constants.density;
      fixDef.restitution = part_constants.restitution;
      fixDef.friction = part_constants.friction;
      fixDef.isSensor = !part_constants.collisions;
      fixDef.filter.groupIndex = -1;
      // Create body
      bodyDef = new b2BodyDef();
      // Assign body position
      bodyDef.position.x = this.player_start.x + this.mirror * part_constants.position.x;
      bodyDef.position.y = this.player_start.y + part_constants.position.y;
      bodyDef.userData = {
        name: 'moto',
        type: this.ghost ? 'ghost' : 'player',
        moto: this
      };
      bodyDef.type = b2Body.b2_dynamicBody;
      // Assign fixture to body and add body to 2D world
      wheel = this.world.CreateBody(bodyDef);
      wheel.CreateFixture(fixDef);
      return wheel;
    }

    create_axle(part_constants) {
      var body, bodyDef, fixDef;
      // Create fixture
      fixDef = new b2FixtureDef();
      fixDef.shape = new b2PolygonShape();
      fixDef.density = part_constants.density;
      fixDef.restitution = part_constants.restitution;
      fixDef.friction = part_constants.friction;
      fixDef.isSensor = !part_constants.collisions;
      fixDef.filter.groupIndex = -1;
      Physics.create_shape(fixDef, part_constants.shape, this.mirror === -1);
      // Create body
      bodyDef = new b2BodyDef();
      // Assign body position
      bodyDef.position.x = this.player_start.x + this.mirror * part_constants.position.x;
      bodyDef.position.y = this.player_start.y + part_constants.position.y;
      bodyDef.userData = {
        name: 'moto',
        type: this.ghost ? 'ghost' : 'player',
        moto: this
      };
      bodyDef.type = b2Body.b2_dynamicBody;
      // Assign fixture to body and add body to 2D world
      body = this.world.CreateBody(bodyDef);
      body.CreateFixture(fixDef);
      return body;
    }

    create_revolute_joint(axle, wheel) {
      var jointDef;
      jointDef = new b2RevoluteJointDef();
      jointDef.Initialize(axle, wheel, wheel.GetWorldCenter());
      return this.world.CreateJoint(jointDef);
    }

    create_prismatic_joint(axle, part_constants) {
      var angle, jointDef;
      jointDef = new b2PrismaticJointDef();
      angle = part_constants.angle;
      jointDef.Initialize(this.body, axle, axle.GetWorldCenter(), new b2Vec2(this.mirror * angle.x, angle.y));
      jointDef.enableLimit = true;
      jointDef.lowerTranslation = part_constants.lower_translation;
      jointDef.upperTranslation = part_constants.upper_translation;
      jointDef.enableMotor = true;
      jointDef.collideConnected = false;
      return this.world.CreateJoint(jointDef);
    }

    update() {
      var visible;
      this.aabb = this.compute_aabb();
      if (!Constants.debug_physics) {
        visible = this.visible();
        this.update_wheel(this.left_wheel, Constants.left_wheel, visible);
        this.update_wheel(this.right_wheel, Constants.right_wheel, visible);
        this.update_left_axle(this.left_axle, Constants.left_axle, visible);
        this.update_right_axle(this.right_axle, Constants.right_axle, visible);
        this.update_body(this.body, Constants.body, visible);
        return this.rider.update(visible);
      }
    }

    update_wheel(part, part_constants, visible) {
      var angle, position, wheel_sprite;
      if (part_constants.position.x < 0) {
        wheel_sprite = this.left_wheel_sprite;
      } else {
        wheel_sprite = this.right_wheel_sprite;
      }
      wheel_sprite.visible = visible;
      if (visible) {
        position = part.GetPosition();
        angle = part.GetAngle();
        wheel_sprite.x = position.x;
        wheel_sprite.y = -position.y;
        wheel_sprite.rotation = -angle;
        return wheel_sprite.scale.x = this.mirror * Math.abs(wheel_sprite.scale.x);
      }
    }

    update_body(part, part_constants, visible) {
      var angle, position;
      this.body_sprite.visible = visible;
      if (visible) {
        position = part.GetPosition();
        angle = part.GetAngle();
        this.body_sprite.x = position.x;
        this.body_sprite.y = -position.y;
        this.body_sprite.rotation = -angle;
        return this.body_sprite.scale.x = this.mirror * Math.abs(this.body_sprite.scale.x);
      }
    }

    update_left_axle(part, part_constants, visible) {
      var axle_position, axle_thickness, texture, wheel_position;
      axle_thickness = 0.09;
      wheel_position = this.left_wheel.GetPosition();
      wheel_position = {
        x: wheel_position.x - this.mirror * axle_thickness / 2.0,
        y: wheel_position.y - 0.025
      };
      // Position relative to center of body
      axle_position = {
        x: -0.17 * this.mirror,
        y: -0.30
      };
      texture = this.ghost ? part_constants.ghost_texture : part_constants.texture;
      return this.update_axle_common(wheel_position, axle_position, axle_thickness, texture, 'left', visible);
    }

    update_right_axle(part, part_constants, visible) {
      var axle_position, axle_thickness, texture, wheel_position;
      axle_thickness = 0.07;
      wheel_position = this.right_wheel.GetPosition();
      wheel_position = {
        x: wheel_position.x + this.mirror * axle_thickness / 2.0 - this.mirror * 0.03,
        y: wheel_position.y - 0.045
      };
      // Position relative to center of body
      axle_position = {
        x: 0.52 * this.mirror,
        y: 0.025
      };
      texture = this.ghost ? part_constants.ghost_texture : part_constants.texture;
      return this.update_axle_common(wheel_position, axle_position, axle_thickness, texture, 'right', visible);
    }

    update_axle_common(wheel_position, axle_position, axle_thickness, texture, side, visible) {
      var angle, axle_adjusted_position, axle_sprite, body_angle, body_position, distance;
      axle_sprite = this[`${side}_axle_sprite`];
      axle_sprite.visible = visible;
      if (visible) {
        body_position = this.body.GetPosition();
        body_angle = this.body.GetAngle();
        // Adjusted position depending of rotation of body
        axle_adjusted_position = Math2D.rotate_point(axle_position, body_angle, body_position);
        // Distance
        distance = Math2D.distance_between_points(wheel_position, axle_adjusted_position);
        // Angle
        angle = Math2D.angle_between_points(axle_adjusted_position, wheel_position) + this.mirror * Math.PI / 2;
        axle_sprite.width = distance;
        axle_sprite.height = axle_thickness;
        axle_sprite.x = wheel_position.x;
        axle_sprite.y = -wheel_position.y;
        axle_sprite.rotation = -angle;
        return axle_sprite.scale.x = this.mirror * Math.abs(axle_sprite.scale.x);
      }
    }

    // estimation of aabb of moto + rider (based on wheels and head)
    compute_aabb() {
      var aabb, lower1, lower2, lower3, upper1, upper2, upper3;
      // lower position of wheels or head (in case or looping)
      lower1 = this.left_wheel.GetFixtureList().GetAABB().lowerBound;
      lower2 = this.right_wheel.GetFixtureList().GetAABB().lowerBound;
      lower3 = this.rider.head.GetFixtureList().GetAABB().lowerBound;
      // upper position of wheels or head (in case or looping)
      upper1 = this.left_wheel.GetFixtureList().GetAABB().upperBound;
      upper2 = this.right_wheel.GetFixtureList().GetAABB().upperBound;
      upper3 = this.rider.head.GetFixtureList().GetAABB().upperBound;
      aabb = new Box2D.Collision.b2AABB();
      aabb.lowerBound.Set(Math.min(lower1.x, lower2.x, lower3.x), Math.min(lower1.y, lower2.y, lower3.y));
      aabb.upperBound.Set(Math.max(upper1.x, upper2.x, upper3.x), Math.max(upper1.y, upper2.y, upper3.y));
      return aabb;
    }

    visible() {
      return this.aabb.TestOverlap(this.level.camera.aabb);
    }

  };

  Particles = class Particles {
    constructor(level, replay) {
      this.level = level;
      this.physics = level.physics;
      this.world = this.physics.world;
      this.list = [];
    }

    create() {
      var bodyDef, fixDef, particle;
      // Create fixture
      fixDef = new b2FixtureDef();
      fixDef.shape = new b2CircleShape(0.04);
      fixDef.density = 1.0;
      fixDef.restitution = 0.5;
      fixDef.friction = 1.0;
      fixDef.isSensor = false;
      fixDef.filter.groupIndex = -1;
      // Create body
      bodyDef = new b2BodyDef();
      // Assign body position
      bodyDef.position.x = this.level.moto.left_wheel.GetPosition().x;
      bodyDef.position.y = this.level.moto.left_wheel.GetPosition().y - Constants.left_wheel.radius;
      bodyDef.userData = {
        name: 'particle'
      };
      bodyDef.type = b2Body.b2_dynamicBody;
      // Assign fixture to body and add body to 2D world
      particle = this.world.CreateBody(bodyDef);
      particle.CreateFixture(fixDef);
      particle.ApplyForce({
        x: -1,
        y: -1
      }, particle.GetWorldCenter());
      return this.list.push(particle);
    }

    update() {
      var ctx, k, len, particle, position, ref, results;
      ctx = this.level.ctx;
      ref = this.list;
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        particle = ref[k];
        position = particle.GetPosition();
        ctx.save();
        ctx.translate(position.x, position.y);
        ctx.beginPath();
        ctx.arc(0, 0, 0.04, 0, 2 * Math.PI);
        ctx.fill();
        results.push(ctx.restore());
      }
      return results;
    }

  };

  // Sere here for replay informations :
  // https://github.com/MichaelHoste/xmoto.js/issues/8
  Replay = class Replay {
    constructor(level) {
      this.level = level;
      this.success = false;
      this.steps = 0;
      this.inputs = {
        up_down: [],
        up_up: [],
        down_down: [],
        down_up: [],
        left_down: [],
        left_up: [],
        right_down: [],
        right_up: [],
        space_pressed: []
      };
      this.key_steps = {};
    }

    clone() {
      var k, key, l, len, len1, new_replay, part, ref, ref1, ref2, value;
      new_replay = new Replay(this.level);
      new_replay.success = this.success;
      new_replay.steps = this.steps;
      ref = ['up_down', 'up_up', 'down_down', 'down_up', 'left_down', 'left_up', 'right_down', 'right_up', 'space_pressed'];
      // Copy inputs
      for (k = 0, len = ref.length; k < len; k++) {
        key = ref[k];
        new_replay.inputs[key] = this.inputs[key].slice(); // copy array
      }
      ref1 = this.key_steps;
      
      // Copy key-steps
      for (key in ref1) {
        value = ref1[key];
        new_replay.key_steps[key] = {};
        ref2 = ['body', 'left_wheel', 'right_wheel', 'left_axle', 'right_axle', 'torso', 'upper_leg', 'lower_leg', 'upper_arm', 'lower_arm'];
        for (l = 0, len1 = ref2.length; l < len1; l++) {
          part = ref2[l];
          new_replay.key_steps[key][part] = {
            position: {
              x: value[part].position.x,
              y: value[part].position.y
            },
            angle: value[part].angle,
            linear_velocity: {
              x: value[part].linear_velocity.x,
              y: value[part].linear_velocity.y
            },
            angular_velocity: value[part].angular_velocity
          };
        }
      }
      return new_replay;
    }

    add_step() {
      this.steps = this.level.physics.steps;
      this.add_inputs();
      return this.add_key_steps();
    }

    add_inputs() {
      var input, k, key, len, ref;
      input = this.level.input;
      ref = ['up', 'down', 'left', 'right'];
      for (k = 0, len = ref.length; k < len; k++) {
        key = ref[k];
        if (input[key] && this.is_up(key)) {
          this.inputs[`${key}_down`].push(this.steps);
        } else if (!input[key] && this.is_down(key)) {
          this.inputs[`${key}_up`].push(this.steps);
        }
      }
      if (input.space) {
        return this.inputs['space_pressed'].push(this.steps);
      }
    }

    add_key_steps() {
      var k, key_step, l, len, len1, moto, part, ref, ref1, results, rider;
      moto = this.level.moto;
      rider = moto.rider;
      if (this.steps % Constants.replay_key_step === 0) {
        key_step = this.key_steps[this.steps.toString()] = {};
        ref = ['body', 'left_wheel', 'right_wheel', 'left_axle', 'right_axle'];
        for (k = 0, len = ref.length; k < len; k++) {
          part = ref[k];
          key_step[part] = this.physics_values(moto[part]);
        }
        ref1 = ['torso', 'upper_leg', 'lower_leg', 'upper_arm', 'lower_arm'];
        results = [];
        for (l = 0, len1 = ref1.length; l < len1; l++) {
          part = ref1[l];
          results.push(key_step[part] = this.physics_values(rider[part]));
        }
        return results;
      }
    }

    // TODO dichotomic search
    last(input) {
      var element, i, input_length, k, last_element, len, ref, steps;
      last_element = null;
      input_length = this.inputs[input].length;
      steps = this.level.physics.steps;
      ref = this.inputs[input];
      for (i = k = 0, len = ref.length; k < len; i = ++k) {
        element = ref[i];
        if (element <= steps && (i + 1 > input_length - 1 || this.inputs[input][i + 1] > steps)) {
          last_element = element;
          break;
        }
      }
      return last_element;
    }

    is_up(key) {
      return this.last(`${key}_up`) >= this.last(`${key}_down`);
    }

    is_down(key) {
      return this.last(`${key}_down`) >= this.last(`${key}_up`);
    }

    is_pressed(key) {
      return this.last(`${key}_pressed`) === this.level.physics.steps;
    }

    load(data) {
      var splitted;
      splitted = data.split("\n");
      this.inputs = ReplayConversionService.string_to_inputs(splitted[0]);
      this.key_steps = ReplayConversionService.string_to_key_steps(splitted[1]);
      this.success = true;
      return this;
    }

    save() {
      var inputs_string, key_steps_string, replay_string;
      inputs_string = ReplayConversionService.inputs_to_string(this.inputs);
      key_steps_string = ReplayConversionService.key_steps_to_string(this.key_steps);
      replay_string = inputs_string + "\n" + key_steps_string;
      return $.post(this.level.options.scores_path, {
        level: this.level.infos.identifier,
        time: this.level.current_time,
        steps: this.steps,
        replay: replay_string
      });
    }

    physics_values(object) {
      return {
        position: {
          x: object.GetPosition().x,
          y: object.GetPosition().y
        },
        angle: object.GetAngle(),
        linear_velocity: {
          x: object.GetLinearVelocity().x,
          y: object.GetLinearVelocity().y
        },
        angular_velocity: object.GetAngularVelocity()
      };
    }

  };

  b2Vec2 = Box2D.Common.Math.b2Vec2;

  b2BodyDef = Box2D.Dynamics.b2BodyDef;

  b2Body = Box2D.Dynamics.b2Body;

  b2FixtureDef = Box2D.Dynamics.b2FixtureDef;

  b2Fixture = Box2D.Dynamics.b2Fixture;

  b2PolygonShape = Box2D.Collision.Shapes.b2PolygonShape;

  b2CircleShape = Box2D.Collision.Shapes.b2CircleShape;

  b2PrismaticJointDef = Box2D.Dynamics.Joints.b2PrismaticJointDef;

  b2RevoluteJointDef = Box2D.Dynamics.Joints.b2RevoluteJointDef;

  Rider = class Rider {
    constructor(level, moto) {
      this.level = level;
      this.assets = level.assets;
      this.world = level.physics.world;
      this.moto = moto;
      this.mirror = moto.mirror;
      this.ghost = moto.ghost;
    }

    destroy() {
      this.world.DestroyBody(this.head);
      this.world.DestroyBody(this.torso);
      this.world.DestroyBody(this.lower_leg);
      this.world.DestroyBody(this.upper_leg);
      this.world.DestroyBody(this.lower_arm);
      this.world.DestroyBody(this.upper_arm);
      this.level.layers.static_level.removeChild(this.head_sprite);
      this.level.layers.static_level.removeChild(this.torso_sprite);
      this.level.layers.static_level.removeChild(this.lower_leg_sprite);
      this.level.layers.static_level.removeChild(this.upper_leg_sprite);
      this.level.layers.static_level.removeChild(this.lower_arm_sprite);
      return this.level.layers.static_level.removeChild(this.upper_arm_sprite);
    }

    load_assets() {
      var k, len, part, parts, results;
      parts = [Constants.torso, Constants.upper_leg, Constants.lower_leg, Constants.upper_arm, Constants.lower_arm];
      results = [];
      for (k = 0, len = parts.length; k < len; k++) {
        part = parts[k];
        if (this.ghost) {
          results.push(this.assets.moto.push(part.ghost_texture));
        } else {
          results.push(this.assets.moto.push(part.texture));
        }
      }
      return results;
    }

    init_physics() {
      this.player_start = this.level.entities.player_start;
      this.head = this.create_head();
      this.torso = this.create_part(Constants.torso, 'torso');
      this.lower_leg = this.create_part(Constants.lower_leg, 'lower_leg');
      this.upper_leg = this.create_part(Constants.upper_leg, 'upper_leg');
      this.lower_arm = this.create_part(Constants.lower_arm, 'lower_arm');
      this.upper_arm = this.create_part(Constants.upper_arm, 'upper_arm');
      this.neck_joint = this.create_neck_joint();
      this.ankle_joint = this.create_joint(Constants.ankle, this.lower_leg, this.moto.body);
      this.wrist_joint = this.create_joint(Constants.wrist, this.lower_arm, this.moto.body);
      this.knee_joint = this.create_joint(Constants.knee, this.lower_leg, this.upper_leg);
      this.elbow_joint = this.create_joint(Constants.elbow, this.upper_arm, this.lower_arm);
      this.shoulder_joint = this.create_joint(Constants.shoulder, this.upper_arm, this.torso, true);
      return this.hip_joint = this.create_joint(Constants.hip, this.upper_leg, this.torso, true);
    }

    init_sprites() {
      var asset_name, k, len, part, ref, results;
      ref = ['torso', 'upper_leg', 'lower_leg', 'upper_arm', 'lower_arm'];
      results = [];
      for (k = 0, len = ref.length; k < len; k++) {
        part = ref[k];
        if (this.ghost) {
          asset_name = Constants[part].ghost_texture;
        } else {
          asset_name = Constants[part].texture;
        }
        this[`${part}_sprite`] = PIXI.Sprite.from(this.assets.get_url(asset_name));
        this[`${part}_sprite`].width = Constants[part].texture_size.x;
        this[`${part}_sprite`].height = Constants[part].texture_size.y;
        this[`${part}_sprite`].anchor.x = 0.5;
        this[`${part}_sprite`].anchor.y = 0.5;
        this[`${part}_sprite`].label = part;
        results.push(this.level.layers.static_level.addChild(this[`${part}_sprite`]));
      }
      return results;
    }

    position() {
      return this.moto.body.GetPosition();
    }

    eject() {
      var adjusted_force_vector, eject_angle, force_vector;
      if (!this.moto.dead) {
        this.level.listeners.kill_moto(this.moto);
        force_vector = {
          x: 150.0 * this.moto.mirror,
          y: 0
        };
        eject_angle = this.mirror * this.moto.body.GetAngle() + Math.PI / 4.0;
        adjusted_force_vector = Math2D.rotate_point(force_vector, eject_angle, {
          x: 0,
          y: 0
        });
        return this.torso.ApplyForce(adjusted_force_vector, this.torso.GetWorldCenter());
      }
    }

    create_head() {
      var body, bodyDef, fixDef;
      // Create fixture
      fixDef = new b2FixtureDef();
      fixDef.shape = new b2CircleShape(Constants.head.radius);
      fixDef.density = Constants.head.density;
      fixDef.restitution = Constants.head.restitution;
      fixDef.friction = Constants.head.friction;
      fixDef.isSensor = !Constants.head.collisions;
      fixDef.filter.groupIndex = -1;
      // Create body
      bodyDef = new b2BodyDef();
      // Assign body position
      bodyDef.position.x = this.player_start.x + this.mirror * Constants.head.position.x;
      bodyDef.position.y = this.player_start.y + Constants.head.position.y;
      bodyDef.userData = {
        name: 'rider',
        type: this.ghost ? 'ghost' : 'player',
        part: 'head',
        rider: this
      };
      bodyDef.type = b2Body.b2_dynamicBody;
      //# Assign fixture to body and add body to 2D world
      body = this.world.CreateBody(bodyDef);
      body.CreateFixture(fixDef);
      return body;
    }

    create_part(part_constants, name) {
      var body, bodyDef, fixDef;
      // Create fixture
      fixDef = new b2FixtureDef();
      fixDef.shape = new b2PolygonShape();
      fixDef.density = part_constants.density;
      fixDef.restitution = part_constants.restitution;
      fixDef.friction = part_constants.friction;
      fixDef.isSensor = !part_constants.collisions;
      fixDef.filter.groupIndex = -1;
      Physics.create_shape(fixDef, part_constants.shape, this.mirror === -1);
      // Create body
      bodyDef = new b2BodyDef();
      // Assign body position
      bodyDef.position.x = this.player_start.x + this.mirror * part_constants.position.x;
      bodyDef.position.y = this.player_start.y + part_constants.position.y;
      // Assign body angle
      bodyDef.angle = this.mirror * part_constants.angle;
      bodyDef.userData = {
        name: 'rider',
        type: this.ghost ? 'ghost' : 'player',
        part: name,
        rider: this
      };
      bodyDef.type = b2Body.b2_dynamicBody;
      // Assign fixture to body and add body to 2D world
      body = this.world.CreateBody(bodyDef);
      body.CreateFixture(fixDef);
      return body;
    }

    set_joint_commons(joint) {
      if (this.mirror === 1) {
        joint.lowerAngle = -Math.PI / 15;
        joint.upperAngle = Math.PI / 108;
      } else if (this.mirror === -1) {
        joint.lowerAngle = -Math.PI / 108;
        joint.upperAngle = Math.PI / 15;
      }
      return joint.enableLimit = true;
    }

    create_neck_joint() {
      var axe, jointDef, position;
      position = this.head.GetWorldCenter();
      axe = {
        x: position.x,
        y: position.y
      };
      jointDef = new b2RevoluteJointDef();
      jointDef.Initialize(this.head, this.torso, axe);
      return this.world.CreateJoint(jointDef);
    }

    create_joint(joint_constants, part1, part2, invert_joint = false) {
      var axe, jointDef, position;
      position = part1.GetWorldCenter();
      axe = {
        x: position.x + this.mirror * joint_constants.axe_position.x,
        y: position.y + joint_constants.axe_position.y
      };
      jointDef = new b2RevoluteJointDef();
      if (invert_joint) {
        jointDef.Initialize(part2, part1, axe);
      } else {
        jointDef.Initialize(part1, part2, axe);
      }
      this.set_joint_commons(jointDef);
      return this.world.CreateJoint(jointDef);
    }

    update(visible) {
      if (!Constants.debug_physics) {
        this.update_part(this.torso, 'torso', visible);
        this.update_part(this.upper_leg, 'upper_leg', visible);
        this.update_part(this.lower_leg, 'lower_leg', visible);
        this.update_part(this.upper_arm, 'upper_arm', visible);
        return this.update_part(this.lower_arm, 'lower_arm', visible);
      }
    }

    update_part(part, name, visible) {
      var angle, position, sprite;
      sprite = this[`${name}_sprite`];
      sprite.visible = visible;
      if (visible) {
        position = part.GetPosition();
        angle = part.GetAngle();
        sprite.x = position.x;
        sprite.y = -position.y;
        sprite.rotation = -angle;
        return sprite.scale.x = this.mirror * Math.abs(sprite.scale.x);
      }
    }

  };

  // The only goal of this class is to flip the moto :

  // Each parameter of each element of the moto is saved, then a flipped moto
  // is created and the parameters are assigned so that the velocity the same

  // Careful, the position of the left wheel is assigned to the one of the
  // right wheel and vice-versa (this is symmetric, it's normal!)
  MotoFlipService = class MotoFlipService {
    static execute(moto) {
      var body, head, left_axle, left_wheel, lower_arm, lower_leg, right_axle, right_wheel, torso, upper_arm, upper_leg;
      body = {
        position: moto.body.GetPosition(),
        angle: moto.body.GetAngle(),
        linear: moto.body.GetLinearVelocity(),
        angular: moto.body.GetAngularVelocity()
      };
      left_wheel = {
        position: moto.left_wheel.GetPosition(),
        angle: moto.left_wheel.GetAngle(),
        linear: moto.left_wheel.GetLinearVelocity(),
        angular: moto.left_wheel.GetAngularVelocity()
      };
      right_wheel = {
        position: moto.right_wheel.GetPosition(),
        angle: moto.right_wheel.GetAngle(),
        linear: moto.right_wheel.GetLinearVelocity(),
        angular: moto.right_wheel.GetAngularVelocity()
      };
      left_axle = {
        position: moto.left_axle.GetPosition(),
        angle: moto.left_axle.GetAngle(),
        linear: moto.left_axle.GetLinearVelocity(),
        angular: moto.left_axle.GetAngularVelocity()
      };
      right_axle = {
        position: moto.right_axle.GetPosition(),
        angle: moto.right_axle.GetAngle(),
        linear: moto.right_axle.GetLinearVelocity(),
        angular: moto.right_axle.GetAngularVelocity()
      };
      head = {
        position: moto.rider.head.GetPosition(),
        angle: moto.rider.head.GetAngle(),
        linear: moto.rider.head.GetLinearVelocity(),
        angular: moto.rider.head.GetAngularVelocity()
      };
      torso = {
        position: moto.rider.torso.GetPosition(),
        angle: moto.rider.torso.GetAngle(),
        linear: moto.rider.torso.GetLinearVelocity(),
        angular: moto.rider.torso.GetAngularVelocity()
      };
      lower_leg = {
        position: moto.rider.lower_leg.GetPosition(),
        angle: moto.rider.lower_leg.GetAngle(),
        linear: moto.rider.lower_leg.GetLinearVelocity(),
        angular: moto.rider.lower_leg.GetAngularVelocity()
      };
      upper_leg = {
        position: moto.rider.upper_leg.GetPosition(),
        angle: moto.rider.upper_leg.GetAngle(),
        linear: moto.rider.upper_leg.GetLinearVelocity(),
        angular: moto.rider.upper_leg.GetAngularVelocity()
      };
      lower_arm = {
        position: moto.rider.lower_arm.GetPosition(),
        angle: moto.rider.lower_arm.GetAngle(),
        linear: moto.rider.lower_arm.GetLinearVelocity(),
        angular: moto.rider.lower_arm.GetAngularVelocity()
      };
      upper_arm = {
        position: moto.rider.upper_arm.GetPosition(),
        angle: moto.rider.upper_arm.GetAngle(),
        linear: moto.rider.upper_arm.GetLinearVelocity(),
        angular: moto.rider.upper_arm.GetAngularVelocity()
      };
      moto.mirror = moto.rider.mirror = -moto.mirror;
      moto.destroy();
      moto.init();
      moto.body.SetPosition(body.position);
      moto.body.SetAngle(body.angle);
      moto.body.SetLinearVelocity(body.linear);
      moto.body.SetAngularVelocity(body.angular);
      // !!! Position and speed of right_wheel and angular velocity of left_wheel it's normal !
      moto.left_wheel.SetPosition(right_wheel.position);
      moto.left_wheel.SetAngle(-left_wheel.angle);
      moto.left_wheel.SetLinearVelocity(right_wheel.linear);
      moto.left_wheel.SetAngularVelocity(-left_wheel.angular);
      // !!! Position and speed of left_wheel and angular velocity of right_wheel it's normal !
      moto.right_wheel.SetPosition(left_wheel.position);
      moto.right_wheel.SetAngle(-right_wheel.angle);
      moto.right_wheel.SetLinearVelocity(left_wheel.linear);
      moto.right_wheel.SetAngularVelocity(-right_wheel.angular);
      moto.left_axle.SetPosition(left_axle.position);
      moto.left_axle.SetAngle(left_axle.angle);
      moto.left_axle.SetLinearVelocity(left_axle.linear);
      moto.left_axle.SetAngularVelocity(left_axle.angular);
      moto.right_axle.SetPosition(right_axle.position);
      moto.right_axle.SetAngle(right_axle.angle);
      moto.right_axle.SetLinearVelocity(right_axle.linear);
      moto.right_axle.SetAngularVelocity(right_axle.angular);
      moto.rider.head.SetPosition(head.position);
      moto.rider.head.SetAngle(head.angle);
      moto.rider.head.SetLinearVelocity(head.linear);
      moto.rider.head.SetAngularVelocity(head.angular);
      moto.rider.torso.SetPosition(torso.position);
      moto.rider.torso.SetAngle(torso.angle);
      moto.rider.torso.SetLinearVelocity(torso.linear);
      moto.rider.torso.SetAngularVelocity(torso.angular);
      moto.rider.lower_leg.SetPosition(lower_leg.position);
      moto.rider.lower_leg.SetAngle(lower_leg.angle);
      moto.rider.lower_leg.SetLinearVelocity(lower_leg.linear);
      moto.rider.lower_leg.SetAngularVelocity(lower_leg.angular);
      moto.rider.upper_leg.SetPosition(upper_leg.position);
      moto.rider.upper_leg.SetAngle(upper_leg.angle);
      moto.rider.upper_leg.SetLinearVelocity(upper_leg.linear);
      moto.rider.upper_leg.SetAngularVelocity(upper_leg.angular);
      moto.rider.lower_arm.SetPosition(lower_arm.position);
      moto.rider.lower_arm.SetAngle(lower_arm.angle);
      moto.rider.lower_arm.SetLinearVelocity(lower_arm.linear);
      moto.rider.lower_arm.SetAngularVelocity(lower_arm.angular);
      moto.rider.upper_arm.SetPosition(upper_arm.position);
      moto.rider.upper_arm.SetAngle(upper_arm.angle);
      moto.rider.upper_arm.SetLinearVelocity(upper_arm.linear);
      return moto.rider.upper_arm.SetAngularVelocity(upper_arm.angular);
    }

  };

  // Convert replay from object to minified string to be send to the server
  // And vice versa
  ReplayConversionService = class ReplayConversionService {
    // String is like : "keyA:199,240,569|keyB:29,40,55..."
    static inputs_to_string(inputs) {
      var k, key, l, len, len1, ref, ref1, step, string;
      string = '';
      ref = ['up_down', 'up_up', 'down_down', 'down_up', 'left_down', 'left_up', 'right_down', 'right_up', 'space_pressed'];
      for (k = 0, len = ref.length; k < len; k++) {
        key = ref[k];
        string += `${key}:`;
        ref1 = inputs[key];
        for (l = 0, len1 = ref1.length; l < len1; l++) {
          step = ref1[l];
          string += `${step},`;
        }
        if (string[string.length - 1] === ',') { // remove last ',' if any
          string = string.slice(0, -1);
        }
        string += '|';
      }
      string = string.slice(0, -1); // remove last '|'
      return LZString.compressToBase64(string);
    }

    static string_to_inputs(string) {
      var i, inputs, k, key, keys, l, len, len1, name, splitted, step, values;
      inputs = {};
      string = LZString.decompressFromBase64(string);
      keys = string.split('|');
      for (k = 0, len = keys.length; k < len; k++) {
        key = keys[k];
        splitted = key.split(':');
        name = splitted[0];
        values = splitted[1].split(',');
        inputs[name] = [];
        if (values[0] !== '') {
          for (i = l = 0, len1 = values.length; l < len1; i = ++l) {
            step = values[i];
            inputs[name][i] = parseInt(step);
          }
        }
      }
      return inputs;
    }

    // String is like : "60@step1=step2=step3=..." where 60 is the key-step interval of the replay
    // step1 is like : "part1_positions|part2_positions|...|part10_positions"
    // part1_positions is like : "11.1234,22.1234,33.1234,44.1234,55.1234,66.1234" (each position and angle values)
    static key_steps_to_string(key_steps) {
      var a, b, c, d, e, f, k, key, key_step, len, ref, step, string;
      string = `${Constants.replay_key_step}@`;
      for (step in key_steps) {
        key_step = key_steps[step];
        ref = ['body', 'left_wheel', 'right_wheel', 'left_axle', 'right_axle', 'torso', 'upper_leg', 'lower_leg', 'upper_arm', 'lower_arm'];
        for (k = 0, len = ref.length; k < len; k++) {
          key = ref[k];
          a = key_step[key].position.x.toFixed(Constants.replay_key_step_precision);
          b = key_step[key].position.y.toFixed(Constants.replay_key_step_precision);
          c = key_step[key].angle.toFixed(Constants.replay_key_step_precision);
          d = key_step[key].linear_velocity.x.toFixed(Constants.replay_key_step_precision);
          e = key_step[key].linear_velocity.y.toFixed(Constants.replay_key_step_precision);
          f = key_step[key].angular_velocity.toFixed(Constants.replay_key_step_precision);
          string += `${a},${b},${c},${d},${e},${f}|`;
        }
        string = string.slice(0, -1); // remove last '|'
        string += '=';
      }
      if (string[string.length - 1] === '=') { // remove last '='
        string = string.slice(0, -1);
      }
      return LZString.compressToBase64(string);
    }

    static string_to_key_steps(string) {
      var current_interval, element, i, k, key_step, key_step_string, key_steps, key_steps_string, l, len, len1, part_string, ref, ref1, step_interval, value_string;
      key_steps = {};
      string = LZString.decompressFromBase64(string);
      key_steps_string = string.split('@')[1];
      step_interval = parseInt(string.split('@')[0]);
      current_interval = step_interval;
      if (key_steps_string.indexOf("=") === -1) {
        // If no "=", then there are no key-steps and we return empty object
        return key_steps;
      }
      ref = key_steps_string.split('=');
      for (k = 0, len = ref.length; k < len; k++) {
        key_step_string = ref[k];
        key_step = {};
        part_string = key_step_string.split('|');
        ref1 = ['body', 'left_wheel', 'right_wheel', 'left_axle', 'right_axle', 'torso', 'upper_leg', 'lower_leg', 'upper_arm', 'lower_arm'];
        for (i = l = 0, len1 = ref1.length; l < len1; i = ++l) {
          element = ref1[i];
          value_string = part_string[i].split(',');
          key_step[element] = {
            position: {
              x: parseFloat(value_string[0]),
              y: parseFloat(value_string[1])
            },
            angle: parseFloat(value_string[2]),
            linear_velocity: {
              x: parseFloat(value_string[3]),
              y: parseFloat(value_string[4])
            },
            angular_velocity: parseFloat(value_string[5])
          };
        }
        key_steps[current_interval] = key_step;
        current_interval += step_interval;
      }
      return key_steps;
    }

  };

  Assets = class Assets {
    constructor() {
      this.theme = void 0;
      this.textures = []; // texture list
      this.anims = []; // anim list
      this.effects = []; // effect list (edge etc.)
      this.moto = []; // moto list
      this.sounds = []; // Sounds
      this.resources = {};
    }

    parse_theme(callback) {
      return this.theme = new Theme("modern.xml", callback);
    }

    load(callback) {
      var item, items, k, l, len, len1, len2, len3, m, o, ref, ref1, ref2, ref3, urls;
      items = [];
      ref = this.textures;
      for (k = 0, len = ref.length; k < len; k++) {
        item = ref[k];
        items.push({
          id: item,
          src: `/data/Textures/Textures/${item.toLowerCase()}`
        });
      }
      ref1 = this.anims;
      for (l = 0, len1 = ref1.length; l < len1; l++) {
        item = ref1[l];
        items.push({
          id: item,
          src: `/data/Textures/Anims/${item.toLowerCase()}`
        });
      }
      ref2 = this.effects;
      for (m = 0, len2 = ref2.length; m < len2; m++) {
        item = ref2[m];
        items.push({
          id: item,
          src: `/data/Textures/Effects/${item.toLowerCase()}`
        });
      }
      ref3 = this.moto;
      for (o = 0, len3 = ref3.length; o < len3; o++) {
        item = ref3[o];
        items.push({
          id: item,
          src: `/data/Textures/Riders/${item.toLowerCase()}`
        });
      }
      items = _.uniqBy(items, 'id');
      urls = _.map(items, function(item) {
        return item.src;
      });
      return PIXI.Assets.load(urls).then(() => {
        var len4, p;
        for (p = 0, len4 = items.length; p < len4; p++) {
          item = items[p];
          this.resources[item.id] = {
            url: item.src
          };
        }
        return callback();
      });
    }

    // Get an asset by its name ("id")
    get(name) {
      return this.resources[name].data;
    }

    get_url(name) {
      return this.resources[name].url;
    }

  };

  // Special debug mode to change constants visually
  bind_debug_button = function() {
    $('#debug .debug-button').on('click', function() {
      window.location = '?level=' + $("#levels option:selected").text() + '&debug=true';
      return false;
    });
    return $('.normal-button').on('click', function() {
      window.location = '?level=' + $("#levels option:selected").text();
      return false;
    });
  };

  bind_delete_params_buttons = function() {
    return $("#debug span.delete").on('click', function() {
      $(this).closest('.template').remove();
      return $('#debug form').submit();
    });
  };

  override_constants_by_url_params = function(params) {
    var array, array_key, array_keys, i, k, key, len, value;
    for (key in params) {
      value = params[key];
      array_keys = key.split('.');
      array = Constants;
      for (i = k = 0, len = array_keys.length; k < len; i = ++k) {
        array_key = array_keys[i];
        if (i === array_keys.length - 1) {
          if (value === '') {
            delete params[key];
          } else {
            if (value === 'true' || value === 'false') {
              array[array_key] = value === 'true';
            } else if (array_key !== 'level') {
              array[array_key] = parseFloat(value);
            }
          }
        } else {
          array = array[array_key];
        }
      }
    }
    return Constants.chain_reaction();
  };

  display_constants = function() {
    var html, k, key, len, ref, ref1, sub_key, sub_value, value;
    // display all the keys with direct link
    html = '<ul>';
    ref = Object.keys(Constants);
    for (k = 0, len = ref.length; k < len; k++) {
      key = ref[k];
      value = Constants[key];
      if (typeof value !== 'object' && typeof value !== 'function') {
        html += `<li><a href=\"${document.URL}&${key}=${value}\">${key}</a> (${value})</li>`;
      } else {
        html += `<li>${key}<ul>`;
        ref1 = Constants[key];
        for (sub_key in ref1) {
          sub_value = ref1[sub_key];
          if (typeof sub_value !== 'object') {
            html += `<li><a href=\"${document.URL}&${key}.${sub_key}=${sub_value}\">${sub_key}</a> (${sub_value})</li>`;
          }
        }
        html += "</ul></li>";
      }
    }
    html += '</ul>';
    $('#debug .variables').html(html);
    // Hide ul without li (object with only other objects as value)
    return $("ul > li > ul:not(:has(> li))").parent().hide();
  };

  create_form_with_url_params = function(params) {
    var key, new_input, results, value;
    results = [];
    for (key in params) {
      value = params[key];
      new_input = $('form.debug .template:first').clone().insertBefore('form.debug input[type=submit]');
      new_input.find('label').attr('for', key).text(key);
      new_input.find('input').attr('name', key).attr('id', key).val(value);
      new_input.show();
      results.push($('form.debug .template:first').hide());
    }
    return results;
  };

  $(function() {
    var params;
    params = $.url().param();
    if (Constants.debug || params.debug === 'true') {
      $('.debug').show();
      $('.debug-button').hide();
      $('body').addClass('debug');
      override_constants_by_url_params(params);
      create_form_with_url_params(params);
      display_constants();
    }
    bind_debug_button();
    return bind_delete_params_buttons();
  });

  Math2D = class Math2D {
    static distance_between_points(point1, point2) {
      var a, b;
      a = Math.pow(point1.x - point2.x, 2);
      b = Math.pow(point1.y - point2.y, 2);
      return Math.sqrt(a + b);
    }

    static angle_between_points(point1, point2) {
      if (point1.y - point2.y === 0) {
        if (point1.y > point2.y) {
          return Math.PI / 2;
        } else {
          return -Math.PI / 2;
        }
      } else {
        if (point1.y > point2.y) {
          return -Math.atan((point1.x - point2.x) / (point1.y - point2.y));
        } else {
          return -Math.atan((point2.x - point1.x) / (point2.y - point1.y)) + Math.PI;
        }
      }
    }

    // Rotate point from angle around axe
    static rotate_point(point, angle, rotation_axe) {
      var new_point;
      return new_point = {
        x: rotation_axe.x + point.x * Math.cos(angle) - point.y * Math.sin(angle),
        y: rotation_axe.y + point.x * Math.sin(angle) + point.y * Math.cos(angle)
      };
    }

    // If shape has 3 collinear vertices, move them around to avoid that
    static not_collinear_vertices(vertices) {
      var i, k, len, size, vertex;
      size = vertices.length;
      for (i = k = 0, len = vertices.length; k < len; i = ++k) {
        vertex = vertices[i];
        if (vertex.x === vertices[(i + 1) % size].x && vertices[(i + 2) % size].x) {
          vertex.x = vertex.x + 0.001;
          vertices[(i + 1) % size].x = vertex.x - 0.001;
        }
        if (vertex.y === vertices[(i + 1) % size].y && vertices[(i + 2) % size].y) {
          vertex.y = vertex.y + 0.001;
          vertices[(i + 1) % size].y = vertex.y - 0.001;
        }
      }
      return false;
    }

  };

  Theme = class Theme {
    constructor(filename, callback) {
      this.filename = filename;
      this.callback = callback;
      this.sprites = [];
      this.edges = [];
      this.textures = [];
      $.ajax({
        type: "GET",
        url: `/data/Themes/${filename}`,
        dataType: "xml",
        success: this.load_theme,
        context: this
      });
    }

    load_theme(xml) {
      var existing, frames, k, len, name, ref, xml_sprite, xml_sprites;
      xml_sprites = $(xml).find('sprite');
      for (k = 0, len = xml_sprites.length; k < len; k++) {
        xml_sprite = xml_sprites[k];
        if ($(xml_sprite).attr('type') === 'Entity') {
          this.sprites[$(xml_sprite).attr('name')] = {
            file: $(xml_sprite).attr('file'),
            file_base: $(xml_sprite).attr('fileBase'),
            file_extension: $(xml_sprite).attr('fileExtension'),
            size: {
              width: parseFloat($(xml_sprite).attr('width')),
              height: parseFloat($(xml_sprite).attr('height'))
            },
            center: {
              x: parseFloat($(xml_sprite).attr('centerX')),
              y: parseFloat($(xml_sprite).attr('centerY'))
            },
            frames: $(xml_sprite).find('frame').length,
            delay: parseFloat($(xml_sprite).attr('delay'))
          };
        } else if ($(xml_sprite).attr('type') === 'EdgeEffect') {
          this.edges[$(xml_sprite).attr('name').toLowerCase()] = {
            file: $(xml_sprite).attr('file').toLowerCase(),
            scale: parseFloat($(xml_sprite).attr('scale')),
            depth: parseFloat($(xml_sprite).attr('depth'))
          };
        } else if ($(xml_sprite).attr('type') === 'Texture') {
          name = $(xml_sprite).attr('name').toLowerCase();
          frames = $(xml_sprite).find('frame').length;
          existing = this.textures[name];
          if (!existing || existing.frames === 0) {
            this.textures[name] = {
              file: $(xml_sprite).attr('file') ? $(xml_sprite).attr('file').toLowerCase() : '',
              file_base: $(xml_sprite).attr('fileBase'),
              file_extension: (ref = $(xml_sprite).attr('fileExtension')) != null ? ref.toLowerCase() : void 0,
              frames: frames,
              delay: parseFloat($(xml_sprite).attr('delay'))
            };
          }
        }
      }
      return this.callback();
    }

    sprite_params(name) {
      return this.sprites[name];
    }

    edge_params(name) {
      return this.edges[name];
    }

    texture_params(name) {
      return this.textures[name];
    }

  };

}).call(this);
